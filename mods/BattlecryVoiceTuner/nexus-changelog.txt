TargetVersion=1.3.0
BaselineVersion=1.2.4

Added explicit migration guidance for custom battlecry files moving from the retired gender subfolders into the canonical flat layout.
Flattened battlecry audio into one folder with gender-aware hero_{gender}_battlecry_{00..14}.wav names while preserving every packaged recording unchanged; custom files must move out of the retired gender subfolders into the new layout.
Loudness-matched all forty-three packaged command voices around -15 LUFS with a -2 dBTP true-peak ceiling while retaining their original pitch and timing.
Added dedicated male and female Recall command pools so Soul and Service Recall Host orders no longer reuse Follow voices.
Made Natural the default pitch processing mode, so the full final shift now uses playback rate unless the player chooses Balanced or Tempo Preserving processing.
Advanced the config schema so untouched Balanced defaults adopt Natural while compatible customized manual pitch, processing, voice-growth, and approved command-voice values remain eligible for conservative recovery.
Added an independent Native Voice Tuning master control while keeping battlecries and command voices independently configurable.
Streamlined FoA Mod Manager with clearer master and acoustic labels, a dedicated advanced voice-growth group, and one consolidated Battlecry group for input and challenge controls.
Removed the Testing config section and its PlayRandomTestSound pseudo-button together with the supporting playback code.

Version 2.4.5
Changed the shared Blood Spell Inner Light intensity default from 1.0 to 0.5 and added configurable per-spell multipliers defaulting to 0.8 for Blood Transfusion, 1.0 for Life Transfusion, and 1.2 for Abhartach's Calling.
Incremented ConfigSchemaVersion from 10 to 11 because the shared intensity default materially changed. Untouched old defaults regenerate at 0.5, while customized compatible intensity, light calibration, matching, and audio values remain eligible for conservative recovery.

Version 2.4.4
Made a hand's blood light turn off instantly when that same hand changes from a blood spell to non-blood equipment.
Kept the configured fade when the blood spell remains equipped, including loadout changes that replace only the opposite hand's weapon and transitions between supported blood spells.

Version 2.4.3
Moved each blood-spell light from its wrist socket to the semantic animated hand marker, with the wrist retained as a compatibility fallback.
Added optional First Person Arms Adjuster 0.3.5 integration so unparented world-space hand lights follow its presentation-only visual offset without per-frame reflection, hierarchy searches, or raycasts.

Version 2.4.1
Moved the blood spell light from the player camera to independent main-hand and off-hand wrist anchors that follow the animated hands.
Enabled each hand light only while that hand has Blood Transfusion, Life Transfusion, or Abhartach's Calling equipped and raised, with cast brightness boosts isolated to the casting hand.
Added InteriorIntensityMultiplier for full interior scenes, defaulting to 1.0 so existing brightness is unchanged until configured.

Version 2.4.0
Added Linux/Proton installation support by packaging plugin folders and runtime assets with portable ZIP paths; Windows and Vortex installs remain unchanged.

Version 2.3.9
Added source-isolated Grail Floating Text consolidation for Blood Magic XP when GFT 1.9.0 or newer is installed.
Consolidates live-drain XP only with live-drain XP and corpse rewards only within the same corpse-quality tier; Blood Magic XP never merges with generic XP or another mod.
Added red magic-icon claims for live-drain XP, with a separate ClaimGrailFloatingTextLiveDrainXP toggle and fallback to separate styled entries on older GFT versions.
Added the optional enabled-by-default Bleed skill-graph preload prepared in 2.3.7, moving its proven cold deserialization cost into gameplay loading without Harmony patches.
Improved the Import Previous Settings action wording so it matches the Turn On control and explains its automatic reset.
Kept ConfigSchemaVersion 10 because this release only adds a setting and existing configs can receive its current default safely.

Version 2.3.7
Work in progress; not yet released.
Improved the Import Previous Settings action text so it matches the Turn On control and clearly explains that the action turns back off automatically.
Added an optional, enabled-by-default Bleed skill-graph preload that moves the proven cold Bleed deserialization cost into gameplay loading.
Kept the Bleed preload in an isolated no-Harmony module so it can be disabled now or removed cleanly if a future game update fixes the underlying load hitch.
Kept ConfigSchemaVersion 10 because this release only adds a setting and existing configs can receive its current default safely.

Version 2.3.6
Made the final Import Previous Settings tab always available with current and compatible backup schema status.
Kept the one-shot import action safe when no compatible backup exists and clarified restart guidance.

Version 2.3.5
Fixed previous-session corpse reticles by reconnecting restored corpse models to their saved locations and retaining their blocked, non-drainable state for the session.
Improved the blood spell inner light's camera placement and sheathing fade, and raised its default intensity to 1.0.
Reduced reflection-cache allocations and strengthened Harmony callback registration.
Added five-second Grail Floating Text notices for config resets and load errors.
Preserves durable light, audio, whitelist, and spell-template settings across schema resets, and adds a conservative Import Previous Settings action for supported backups.

Version 2.3.4
Preserves blood spell inner-light intensity, range, and fade plus corpse-leech volume and pitch across future config schema resets.
Preserves blood whitelist terms and exact Blood/Life Transfusion and Abhartach template GUID overrides across future schema resets.
Restored numeric values are parsed invariantly and clamped to their current supported ranges; invalid values are skipped.

Version 2.3.3
Requests Grail Floating Text's five-second System duration for config schema resets and load-time errors.

Version 2.3.2
Fixed restored corpse location aliases expiring after two minutes, which caused previous-session corpses with generic hitbox names such as Ogres to lose the base corpse reticle.
Kept restored corpses registered for the current play session while they remain blocked and non-drainable.

Version 2.3.1
Hid ConfigSchemaVersion from FoA Mod Manager while keeping it in the config file for safe schema resets.

Version 2.3.0
Fixed previous-session corpse reticle detection by reconnecting restored corpse models to their saved parent locations after restoration instead of relying on loot or collider names.
Kept restored corpses non-drainable and reported them to Dishonored Dynamic Crosshair as blocked for the default-size base-color corpse icon.
Changed the default Blood Spell Inner Light intensity from 0.75 to 1.0.
Bumped ConfigSchemaVersion to 10 so the new inner-light default generates cleanly.

Version 2.2.9
Shows successful config schema resets through Grail Floating Text with the system icon and VeryLong duration bucket.

Version 2.2.8
Reduced repeated reflection-cache key allocations by using typed keys for property, field, and method-by-arity lookups.
Replaced owned Harmony callback and transpiler helper name strings with compiler-checked references.

Version 2.2.7
Moved the blood spell inner light back to the camera plane while preserving its vertical offset.
Added default-size base-color corpse reticle feedback for unregistered corpse-like bodies, including corpses loaded from earlier play sessions.
Kept ConfigSchemaVersion 9 because no config settings changed.

Version 2.2.6
Changed the blood spell inner light to begin its configured fade as soon as raised hands enter the sheathed state, without changing casting or other readiness timing.
Kept ConfigSchemaVersion 9 because no config settings changed.

Version 2.2.5
Changed corpse quality tiers for both leech sounds and Grail Floating Text labels to quartile bands: Meager, Worthy, Potent, and Prime.
Kept ConfigSchemaVersion 9 because no config settings changed.

Version 2.2.4
Changed Grail Floating Text corpse XP claims to include the corpse quality label, for example +42 XP (Worthy).
Mapped corpse labels to the existing quality/audio tiers: Meager, Worthy, Potent, and Prime.
Changed the integration to require Grail Floating Text API v6 text XP claims.
Kept ConfigSchemaVersion 9 because no config settings changed.

Version 2.2.3
Changed corpse-leech and live-drain XP to use the game's vanilla effective kill XP basis, including lower-level enemy XP falloff, instead of raw enemy base XP.
Kept corpse quality based on base kill XP and max health so quality scaling remains an enemy-power signal.
Kept ConfigSchemaVersion 9 because no config settings changed.

Version 2.2.1
Added optional Grail Floating Text load-time error notices with the debug icon for startup failures.
Disabled the plugin cleanly after startup patch failures instead of continuing as if fully loaded.

Version 2.2.0
Changed the 3x blood spell inner-light boost to begin 0.3 seconds after cast start while preserving the current cast-end fade-down behavior.
Kept ConfigSchemaVersion 9 because no config settings changed.

Version 2.1.9
Changed the 3x blood spell inner-light boost to fade down over a short 0.25 second ramp instead of snapping back to readied brightness.
Fixed post-cast boost flicker by suppressing all cast-evidence refreshes during the fade-down window while still allowing a real new cast start to boost immediately.
Kept readied inner-light state alive through weapon-hide callbacks when the magic layer is still raised to reduce off/on light flapping.
Kept ConfigSchemaVersion 9 because no config settings changed.

Version 2.1.8
Added optional Grail Floating Text integration for corpse-leech XP, claiming the next XP stat gain as a red corpse-icon floating text entry when Grail Floating Text 1.4.8 or newer is loaded.
Kept live-drain XP on the generic XP notification path so each live tick remains its own normal XP entry.
Bumped ConfigSchemaVersion to 9 so the new integration toggle generates cleanly.

Version 2.1.7
Changed Blood Spell Inner Light defaults to 0.75 intensity and 5.0 range.
Changed the blood spell inner-light cast boost from 2x to 3x and made it ramp in immediately.
Added MagicFSM end/cancel hooks so the boost can ramp down as soon as held casting ends or cancels instead of waiting only for the perform-cast callback.
Bumped ConfigSchemaVersion to 8 so the new inner-light defaults generate cleanly.

Version 2.1.6
Changed the 2x blood spell inner-light boost to wait 0.5 seconds after cast start before ramping in.
Changed performed-cast callbacks to close the boost window so the ramp-down starts at release instead of lingering after the cast.
Kept ConfigSchemaVersion 7 because no config settings changed.

Version 2.1.5
Fixed actual Blood/Life and Abhartach cast callbacks so they refresh the readied inner-light state before applying the 2x spellcast brightness boost.
Stopped the MagicFSM.OnUpdate held-state grace path from starting a 2x boost before real cast or charge evidence.
Kept ConfigSchemaVersion 7 because no config settings changed.

Version 2.1.4
Fixed the red blood spell inner light readiness check so an active magic layer in equip, idle, movement, or cast states keeps the normal light on while blood spell hands are raised.
Stopped MagicFSM.OnUpdate readiness probes from triggering the 2x cast brightness boost; the boost now stays tied to actual casting and charging evidence.
Kept ConfigSchemaVersion 7 because no config settings changed.

Version 2.1.3
Made the red blood spell inner light more reliable when weapon show/hide callbacks flap by keeping a short readied-state grace window and refreshing readiness from SpellAttackHeld when the blood spell is actually held raised.
Changed the 2x cast brightness boost to ramp up over 0.5 seconds and ramp down over 0.5 seconds.
Kept ConfigSchemaVersion 7 because no config settings changed.

Version 2.1.2
Cleaned up the red blood spell inner light implementation by reducing readied-state tracking to the active spell summary and removing old held-probe diagnostics from the earlier activation experiments.
Reused the existing reflection setter path for HDRP light data setup instead of keeping a separate one-off member setter block.
Clarified the generated config comments for the readied-hands behavior and 2x cast boost.
Kept ConfigSchemaVersion 7 because no config settings changed.

Version 2.1.1
Changed the red blood spell inner light to turn on when a matching blood spell is equipped and the magic hands are raised, then turn off when hands are lowered or weapons are hidden.
Changed blood spell cast events to provide a temporary 2x brightness boost instead of activating the light by themselves.
Kept ConfigSchemaVersion 7 because no config settings changed.

Version 2.1.0
Scaled the red blood spell inner light's configured brightness into HDRP-scale render intensity so the enabled custom light is visibly strong in game.
Added runtime HDRP additional light data setup and all-layer light culling for the custom red light when the renderer component is available.
Kept ConfigSchemaVersion 7 because no config settings changed.

Version 2.0.9
Changed the red blood spell inner light to stay active for a short minimum window after confirmed blood-spell activation instead of depending only on fragile per-frame MagicFSM raised-state flags.
Kept ConfigSchemaVersion 7 because no settings changed.

Version 2.0.8
Fixed the red blood spell inner light turning off while Blood Transfusion is still raised after the game clears SpellAttackHeld.
Kept ConfigSchemaVersion 7 because no settings changed.

Version 2.0.7
Added limited blood spell inner light diagnostics for activation, camera resolution, and visibility transitions.
Changed the custom red light to sit slightly forward and below the player camera for better visibility.
Added active camera fallback when Camera.main is unavailable during a raised blood spell.
Bumped ConfigSchemaVersion to 7 so the diagnostic setting generates cleanly.

Version 2.0.6
Added a configurable red no-shadow inner player light while Blood Transfusion, Life Transfusion, or Abhartach's Calling is raised.
Added Blood Spell Inner Light config for enablement, intensity, range, and fade time.
Kept the custom light separate from the vanilla HeroLight so No Player Light remains compatible.
Bumped ConfigSchemaVersion to 6 so the new visual settings generate cleanly.

Version 2.0.5
Added per-quality-tier no-repeat memory for corpse leech sounds.
Added subtle FMOD channel pitch randomization for corpse leech audio.
Added config for repeat avoidance, recent sound memory, and pitch variation.
Kept quality-matched low, medium, high, and max sound pools unchanged.
Bumped ConfigSchemaVersion to 5 so the new Audio settings generate cleanly.

Version 2.0.4
Changed corpse leech audio to choose low, medium, high, or max sound pools from the drained corpse's quality.
Added five WAV slots per quality tier: corpse_leech_low, corpse_leech_medium, corpse_leech_high, and corpse_leech_max.
Simplified corpse leech audio config to an enable switch and one global volume.
Removed prefix, slot count, cooldown, and pitch randomization config from the generated Audio section.
Bumped ConfigSchemaVersion to 4 so the new audio config is generated cleanly.

Version 2.0.3
Added FMOD corpse leech audio for successful Blood/Life corpse ritual completions.
Added random selection from corpse_leech_1.wav through corpse_leech_6.wav in the plugin audio folder.
Added subtle per-play pitch randomization, configurable volume, cooldown, prefix, and slot count.
Bumped ConfigSchemaVersion to 3 so the new Audio section is generated cleanly.

Version 2.0.2
Changed corpse quality from taking the higher of kill XP or max health to a 50/50 blend when both values are known.
Preserved single-signal fallback behavior when only kill XP or only max health can be resolved.
Kept corpse XP payout separate from corpse quality; quality still scales corpse healing, Abhartach effects, and Dynamic Crosshair size data only.
Bumped ConfigSchemaVersion to 2 so generated config comments reflect the new equal-weight quality model.

Version 2.0.1
Made Enabled a true master switch for live drain, Blood/Life spell tuning, and Abhartach tuning.
Added active ConfigSchemaVersion enforcement so missing or changed schemas back up the old config and regenerate clean defaults.
Added conservative cleanup for completed corpse states, destroyed Unity-object aliases, and expired held-cast state.
Updated README and Nexus wording for the current Blood Magic Expansion identity and paired Crosshair release.
Kept ConfigSchemaVersion 1 and the existing 2.0.0 gameplay balance.

Version 2.0.0
Renamed the mod, plugin folder, DLL, GUID, config file, namespace, and API path to Blood Magic Expansion.
Added the new API path BloodMagicExpansion.BloodMagicApi v4 and removed reliance on old BloodMage/BloodMageExpansion technical identities.
Reworked the generated config around FoA Mod Manager readability with Core, Main Loop, Corpse Quality, Bloodless Filter, advanced tuning, performance, and diagnostics sections.
Renamed RewardPreset to Preset and moved the most useful preset/feature toggles into the first two config sections.
Started from a clean ks.tgfoa.blood-magic-expansion.cfg config path with no old config migration.
Kept the existing 1.9.9 gameplay balance, corpse quality behavior, focused corpse states, and Patch 1.25 Mono compatibility.

Version 1.9.9
Added ConfigSchemaVersion; older configs are backed up and regenerated with clean current defaults.
Replaced old per-setting config migration with the schema reset policy.
Added Blood Mage API v4 focused corpse state reporting for none, usable, channeling, spent, and blocked corpses.
Removed legacy corpse-leech API wrapper namespaces; current integrations use BloodMagicExpansion.BloodMagicApi v4.
Kept corpse quality as effect/scale data only; it no longer has to imply whether a corpse is focused.

Version 1.9.8
Retuned live-drain XP ticks to 4% every 1s for Blood Rite, 8% every 1.5s for Desecration, and 12% every 2s for Soul Feast.
Kept live-drain caps, corpse ritual timing, healing, corpse quality, and spell tuning values unchanged.

Version 1.9.7
Reversed preset ritual timing so Blood Rite is the fast 1s drain, Desecration stays 1.5s, and Soul Feast becomes the slower 2s high-payout drain.
Matched live-drain tick intervals to the same timing ladder while leaving payout, healing, corpse quality, and spell tuning values unchanged.

Version 1.9.6
Retuned corpse quality references to 300 kill XP and 600 max health so early T1 monsters no longer cap quality.
Expanded Blood Mage corpse quality gameplay effects from 0.5x to 1.5x.
Added Wyrdspawn, Wyrdspirit, Wyrd Spirit, WyrdSlime, Wyrd Slime, and Wyrdness to the default bloodless blacklist while leaving Wights eligible.
Added config migration for old default quality values and missing Wyrd blacklist terms.
Added a Nexus/README quality table tracking intended effect and reticle scaling bands.

Version 1.9.5
Changed unresolved focused corpse quality fallback from 0.5 to 0 so unknown low-tier corpses render at normal Magic reticle size.
Added configurable FallbackQuality under Corpse Quality.
Added LogCorpseQuality diagnostics for focused corpse XP, max health, quality, and fallback status.

Version 1.9.4
Renamed the Abhartach config section from "Abhartach's Calling" to "Abhartach Calling" because BepInEx rejects apostrophes in section and key names.
Verified all Blood Mage config section and key names are BepInEx-safe.
Keeps the 1.9.3 config fallback and 1.9.2 guarded Patch 1.25 diagnostics.

Version 1.9.3
Fixed a startup failure when BepInEx provides a null plugin Config object before Blood Mage binds settings.
Added a fallback ConfigFile resolver that uses the normal ks.tgfoa.blood-mage.cfg path.
Logs whether Blood Mage is using the normal BepInEx plugin config or the fallback config file.
Keeps the 1.9.2 guarded patch diagnostics for the next Patch 1.25 compatibility check.

Version 1.9.2
Added an early startup breadcrumb before config binding so Blood Mage failures are visible in BepInEx logs.
Wrapped config binding and each reflected game patch group with defensive exception logging.
Let one broken 1.25 patch target fail independently instead of silently preventing the whole plugin from finishing startup.
Added a null-safe update guard for failed startup cases.

Version 1.9.1
Added modest corpse quality effect scaling for Blood/Life Transfusion corpse healing.
Added corpse quality scaling for Abhartach's Calling explosion damage, explosion radius, bleed buildup, and held corpse healing.
Added short Abhartach corpse-quality memory so delayed explosion/healing effects can use the corpse quality aimed at during cast.
Added Blood Mage API v3 focused corpse quality effect multiplier reporting.

Version 1.9.0
Added Blood Mage API v2 corpse action reporting for Blood/Life Transfusion and Abhartach's Calling integrations.
Added focused corpse quality reporting from resolved kill XP and enemy max health for Dynamic Crosshair scaling.
Added Corpse Quality config references for kill XP and max health normalization.
Kept the existing Patch 1.25 / build 24270691 compatibility behavior.

Version 1.8.2
Rebuilt against Tainted Grail: The Fall of Avalon Patch 1.25 / build 24270691.
Verified the key reflected Harmony targets still exist in the updated game assemblies.
Kept existing Blood Transfusion, Life Transfusion, live drain, corpse reward, and Abhartach tuning behavior.
No Vortex deployment or profile state changes are included.

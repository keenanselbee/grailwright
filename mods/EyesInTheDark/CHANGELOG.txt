Version 1.4.0
Converted the gameplay preset selector from a one-shot action into a durable applied preset. Uneasy Night, Watchful Night, and Cursed Night now write every governed value shown in three adjacent preset sections; manually editing any governed value selects Custom, and choosing Custom directly changes nothing.
Deferred FoA Mod Manager refresh until the next frame after preset changes so the manager shows the applied live values instead of stale originals marked as modified.
Advanced ConfigSchemaVersion to 23 because ApplyPreset changed from an excluded one-shot action defaulting to Custom into a durable selector defaulting to Watchful Night. Existing customized gameplay values are conservatively retained as Custom during schema-22 recovery so the new default cannot overwrite them.

Version 1.3.9
Clarified the FoA Mod Manager description for Guard Assistance Radius so it explains both the Hero-to-guard and guard-to-hunter response gates. ConfigSchemaVersion remains 22 because this is a description-only change.

Version 1.3.8
Limited guard assistance to pairs where both the guard is within the configured radius of the Hero and the exact official hunter is within that same radius of the guard. Distant hunters can no longer pull settlement guards beyond the intended response area.

Version 1.3.7
Kept guard-awareness scans active for an existing official hunt when the Hero enters Wyrd protection in the same exterior. Hero-target reacquisition remains limited to exposed pursuit, while guards can continue responding to the live Wyrdnight monsters.

Version 1.3.6
Added optional nearby-guard assistance for active official hunts. Ordinary non-unique guards can join combat against every exact Eyes-spawned hunter, including sidecars, while unique, quest, story, bodyguard, and other special actors remain excluded.
Used native combat entry first with a temporary hit-response-style antagonism fallback, capped and deduplicated assistance per hunt, and no permanent faction or settlement-wide changes. Ambient stalkers remain outside this system.
Added EnableGuardAssistance, GuardAssistRadiusMeters, and MaximumAssistingGuards with defaults of true, 24 meters, and four guards. ConfigSchemaVersion remains 22 because all three settings are additive.

Version 1.3.5
Removed numeric prefixes from all stored config sections and standardized Diagnostics as the penultimate FoA Mod Manager section before Import Previous Settings.
Advanced the config schema to 22 because the stored section names changed; schema-21 configs are backed up and customized durable values are translated into the clean section layout during recovery.

Version 1.3.4
Added ShowGrailFloatingTextDiagnostics, enabled by default and active only while Diagnostics is enabled, so concise in-game summaries can be disabled without losing detailed BepInEx logging.
Kept atmospheric Grail Floating Text notifications independent under their existing EnableNotifications and preset controls.

Version 1.3.3
Claimed GFT's built-in Wyrdnight transition event while Atmospheric or Detailed notifications are active, preventing duplicate nightfall and dawn messages while preserving automatic GFT fallback when Eyes notifications are inactive.

Version 1.3.2
Added optional Blood Magic Expansion integration that raises Wyrd Threat only after a corpse ritual completes successfully while the Hero is exposed outdoors during an active Wyrdnight.
Added CorpseDrainThreatAtAverageQuality with an 8-threat default and linear corpse-quality scaling from 4 threat at zero quality to 12 at maximum quality.

Version 1.3.1
Moved rest-clock rotation, dial labels, popup time formatting, and quick-menu time formatting to Glorious UI's independently toggleable Sensible Rest Menu.
Kept Wyrdnight fireplace REST-button availability in Eyes and renamed its setting to ShowWyrdnightRestAvailability so it controls only that presentation.
Incremented ConfigSchemaVersion from 20 to 21 because OwnRestMenu and RestClockLabelFormat were removed and the replacement availability setting has a narrower meaning. Compatible gameplay, threat, encounter, HUD, visual, and notification settings remain eligible for conservative recovery.

Version 1.3.0
Made EnableWyrdnightVisuals control world presentation only, leaving the independently configured threat-meter palette, brightness response, and color shift active.
Incremented ConfigSchemaVersion from 19 to 20 because EnableWyrdnightVisuals now has a narrower world-only meaning. Compatible same-name settings remain eligible for conservative recovery.

Version 1.2.9
Separated world and threat-meter threat response settings so HUD brightness and color changes no longer depend on the world visual controls or the EnableWyrdnightVisuals toggle.
Added independent low- and high-threat meter brightness scales plus separate Purple and Orange maximum meter color-shift strengths. Constant palette brightness remains independently configurable.
Renamed the world response settings to MinimumWorldThreatBrightnessScale, MaximumWorldThreatBrightnessScale, WorldThreatTargetColor, and MaximumWorldThreatColorShift so each control has one explicit purpose.
Incremented ConfigSchemaVersion from 18 to 19 because the four shared world settings were renamed and narrowed to world-only behavior. Schema recovery adopts the current defaults for the renamed world controls and new meter-response controls while preserving compatible same-name palette, gameplay, HUD, and other durable settings.

Version 1.2.8
Replaced the cloned mana bar's blue-tinted fill, glow, and mask artwork with its game-owned neutral white variant and set the Eyes-owned animated material color to white, keeping configured Purple and Orange threat-meter colors clear while preserving the corrected animation direction.
Added separate Purple and Orange threat-meter red target settings, each defaulting to #FF3028, so every palette now owns both ends of its threat color transition.
Incremented ConfigSchemaVersion from 17 to 18 because the existing ThreatRedColor setting now applies only to world visuals instead of also controlling the meter. Compatible customized world and meter settings remain eligible for recovery, while the new palette-specific meter targets adopt their current defaults.

Version 1.2.7
Recalibrated the separate Purple and Orange threat-meter brightness controls so every setting point applies 3x RGB before the shared threat scale. Both retain a 0-to-3 range, the new 1.0 default matches the old 3.0 result, and the new 3.0 maximum reaches 9x RGB.
Incremented ConfigSchemaVersion from 16 to 17 because both same-name brightness settings now have a different scale and default. Schema recovery adopts the new 1.0 baseline for both controls instead of preserving incompatible older values, while unrelated durable settings remain eligible for recovery.

Version 1.2.6
Added separate Purple and Orange threat-meter brightness settings with the existing 1.5 multiplier as both defaults and a 0-to-3 range.
Applied the active palette's brightness before the shared threat scale so live palette and brightness changes update the meter without affecting threat, fill, or world lighting. ConfigSchemaVersion remains 16 because both settings are additive.

Version 1.2.5
Preserved the vanilla Hero-bar shader animation direction on the horizontally and vertically mirrored Wyrd Threat meter by reversing the known VFX_UI speed axes only on a private meter material.
Destroyed the private material with the meter during HUD release and rebuilds, leaving the shared health, mana, and stamina materials unchanged. ConfigSchemaVersion remains 16 because no setting changed.

Version 1.2.4
Replaced the separate Purple exposure multiplier and night-brightness controls with one palette-aware Wyrdnight Brightness setting directly below Wyrdness Palette. Its default value of 1 maps Purple Wyrdness to 1.75x exposure plus 0.35 EV while leaving Orange Wyrdness at native 1x exposure and 0 EV.
Removed the Purple Indirect Diffuse Multiplier setting and stopped patching the game's indirect diffuse lighting, leaving its native value untouched.
Renamed the Native Orange display choice to Orange Wyrdness and added separate Purple and Orange threat-meter base colors selected automatically by the active palette.
Incremented ConfigSchemaVersion from 15 to 16 because the Purple exposure, EV compensation, indirect-diffuse, and single threat-meter color settings were removed or replaced. Compatible durable settings remain eligible for conservative recovery; retired values are intentionally skipped.

Version 1.2.3
Fixed daylight state changes such as pausing, unpausing, and loading from replaying randomized dawn GFT notifications; dawn atmosphere now requires a confirmed Wyrdnight-to-daylight transition.
ConfigSchemaVersion remains 15 because no configuration setting or meaning changed.

Version 1.2.2
Added configurable visual-only threat smoothing with a two-active-second default half-life, preventing battlecries and other brief threat jumps from abruptly recoloring world lighting.
Kept authoritative threat, the HUD meter, hunts, notifications, and dynamic Wyrdnight duration immediate while reusing the existing five-times-per-second visual update.
ConfigSchemaVersion remains 15 because ThreatVisualSmoothingSeconds is additive and existing configurations can safely receive its default.

Version 1.2.1
Added Linux/Proton installation support by packaging plugin folders and runtime assets with portable ZIP paths; Windows and Vortex installs remain unchanged.

Version 1.2.0
Made diagnostics refresh when any Purple exposure or indirect-diffuse control changes live, and included their exact values in the concise visual-state summary.
Updated the consolidated acceptance candidate to use Battlecry Voice Tuner 1.0.7.
ConfigSchemaVersion remains 15 because no configuration setting or meaning changed.

Version 1.1.9
Made the Purple exposure multiplier independently configurable with a 1.2 default and a 0-to-3 range. It remains separate from Purple Night Brightness EV compensation and the Purple indirect diffuse multiplier.
Removed the ineffective mirrored-meter TextureScroller reversal and its private-material lifecycle. Runtime evidence showed the cloned mana-bar hierarchy had no eligible TextureScroller, so the code never altered the visible animation.
Removed claims that the current meter corrects shader-driven animation direction. ConfigSchemaVersion remains 15 because the exposure setting is additive and the removed meter code owned no configuration.

Version 1.1.8
Added a configurable Purple Indirect Diffuse Multiplier with a 1.10 default and a 0-to-3 range.
Applied the multiplier to the game-owned Indirect Lighting Controller after its native Wyrdnight update, following the existing presentation fade while leaving Native Orange, direct moonlight, reflections, exposure, gamma, colors, and global volumes unchanged.
ConfigSchemaVersion remains 15 because the new setting is additive.

Version 1.1.7
Added configurable Purple Night Brightness with a +0.35 EV default and a -2-to-+2 EV range. The setting follows the existing Wyrdnight presentation fade and remains independent of threat.
Applied Purple brightness after Light Control using the active exposure mode's native sign convention: automatic and physical-camera compensation add EV, while fixed exposure subtracts EV.
Kept HDRP post-exposure, gamma, global volumes, colors, and Native Orange exposure untouched. ConfigSchemaVersion remains 15 because the new setting is additive.

Version 1.1.4
Removed the Purple Wyrdness Brightness setting while retaining the configurable 0.8-to-1.2 shared threat visual-strength response.
Added a fixed 1.2 Purple Wyrdness exposure multiplier that follows the existing dusk, dawn, load, and disable presentation blend, remains independent of threat, and leaves Native Orange unchanged. Light Control exposure multipliers remain additive.
Made the rotated rest-clock hand and fill idempotent by assigning one fixed half-turn from their captured native rotations on every refresh, and restored those rotations when Eyes releases the rest menu.
Reduced the default diagnostics GFT System cooldown from 3 seconds to 1 second so closely timed discrete actions such as battlecries are less likely to be hidden by the shared notification lane.
Incremented ConfigSchemaVersion from 14 to 15 because PurpleWyrdnessBrightness was removed and the existing GftSystemCooldownSeconds default changed from 3 to 1 second. Compatible durable customizations remain eligible for conservative recovery; the retired brightness value is intentionally skipped.

Version 1.1.2
Kept the configured moon, moonlight, and full-sky tint strengths independent of the shared 0.8-to-1.2 threat scale, so low threat retains the intended base palette instead of blending back toward the original game colors.
Applied threat and Purple Wyrdness brightness to the selected HDR colors in linear color space, preserving their hue more reliably as illumination changes and threat rises.
Stopped modifying the original skybox emission multiplier. Purple Wyrdness Brightness now scales the purple-tinted full-sky color and nighttime world illumination without boosting neutral or regional sky emission.
Incremented ConfigSchemaVersion from 13 to 14 because PurpleWyrdnessBrightness now controls tinted-sky brightness rather than original sky emission. Compatible durable customizations, including genuine custom brightness values, remain eligible for conservative recovery.

Version 1.1.1
Added a diagnostics-only world-timescale override with a fixed 0.01-to-5.0 vanilla-clock multiplier for quickly testing phase changes, loads, and transitions.
Made the override independent of normal Dynamic Timescale while still governed by the Eyes master switch, with safe restoration and external-clock ownership protection unchanged.
Excluded both override controls from config recovery and previous-settings import so testing state cannot silently return. ConfigSchemaVersion remains 13 because both settings are additive.

Version 1.1.0
Preserved the vanilla Hero-bar animation direction on the horizontally and vertically mirrored Wyrd Threat meter by reversing only the cloned texture-scroller axes affected by the mirror.
Isolated every cloned animated meter image onto its own runtime material so correcting the threat-meter animation cannot reverse or otherwise modify the original health, mana, or stamina bar effects.
Tracked and destroyed the private runtime materials with the meter to avoid material leaks across HUD rebuilds. ConfigSchemaVersion remains 13 because no setting changed.

Version 1.0.9
Reworked unprotected Wyrdnight sleep around the native rest-interruption pipeline. Native interruptions remain authoritative, while Eyes adds configurable threat-scaled risk only when the game did not already wake the Hero.
Made Eyes interruption risk cumulative across one Wyrdnight, so repeated short rests share the same hidden roll and exposure threshold instead of rerolling. Canceling the rest menu adds no exposure.
Applied rest behavior through gameplay presets. Uneasy permits exposed rest with no added Eyes risk, Watchful permits it with a 45-to-75-percent threat-scaled risk, and Cursed uses 80 to 100 percent risk while preventing new exposed rest after Wyrdnight has begun.
Made fueled protective boundaries bypass the added interruption risk and active-night rest gate. After any interruption during unprotected Wyrdnight exposure, further exposed rest is locked until dawn.
Made an Eyes-driven sleep interruption commit one immediate official hunt after the native wake transition. Regional eligibility, player level, elite rules, encounter budget, atomic placement, and zero-cost failure handling remain authoritative; a native interruption never receives a duplicate Eyes hunt.
Added OwnRestMenu, enabled by default. Disabling it leaves rest-menu controls, clock rotation, clock labels, popup time formatting, and disabled-button presentation native while retaining silent gameplay rest rules at the final accepted-rest guard.
Incremented ConfigSchemaVersion from 12 to 13 because AllowUnprotectedWyrdnightRest changed from disabled to enabled by default as part of the new Watchful rest model. Compatible durable customizations remain eligible for conservative recovery, and the new rest-risk and rest-menu settings are additive.

Version 1.0.8
Rotated the complete rest selector by half a day so noon is at the top, midnight is at the bottom, and the hand, fill, mouse, and controller radial selection remain aligned with the displayed time.
Applied the selected 12-hour format to the rest popup's Current time and Resting until values as well as its dial and quick-use clock. TwentyFourHour continues to leave native time text unchanged.
Reduced Wyrdnight lighting overhead by recalculating presentation at the existing five-times-per-second state cadence, caching parsed colors and computed light values, and using the per-frame native hook only to reapply values the game overwrites.
Coalesced and limited expensive global-illumination environment refreshes to four per second during changing sky presentation, with an immediate final refresh when restoring vanilla lighting.
Disabled the layered boundary pass whenever native Wyrd intensity is zero, eliminating its three fullscreen draws while the boundary is inactive.
Cached ambient-stalker renderers and removed repeated renderer and bounds-point array allocations from visibility checks.
Stopped rewriting unchanged threat-meter layout and visibility state, moved world-clock and listener identity polling to the existing five-times-per-second cadence, and aggregated continuous movement diagnostics instead of writing sprint threat to the log five times per second.

Version 1.0.7
Changed the default dynamic Wyrdnight range from 5-15 minutes to 6-12 minutes, keeping a quiet night close to the game's approximately 6.2-minute baseline while allowing maximum threat to double its duration.
Reorganized FoA Mod Manager into a concise primary flow for General, World Clock, HUD, Boundary Appearance, Wyrdnight Appearance, and Notifications, followed by clearly labeled Advanced tuning sections.
Moved the Ambient Stalkers and Allow Elite Enemies toggles into General, made the gameplay preset's one-shot behavior explicit, removed its ordering collision with Time Display, and identified Watchful Night as the recommended tuning.
Added units directly to time and distance labels and replaced exposed implementation terms such as hazard, danger budget, and sidecar with hunt pressure, encounter budget, and additional hunter wording.
Replaced the raw BoundaryHdrIntensity value with normalized BoundaryBrightness, default 1.0 and range 0-3. Runtime converts it through the preserved 271.529 vanilla-equivalent HDR baseline, so the default rendered boundary is unchanged.
Incremented ConfigSchemaVersion from 11 to 12 because the existing Wyrdnight duration defaults changed and BoundaryHdrIntensity was replaced with a differently scaled BoundaryBrightness setting. Compatible durable customizations remain eligible for conservative recovery; the retired raw boundary value is intentionally skipped.

Version 1.0.6
Applied the selected 12-hour clock format to the quick-use weather clock with AM/PM text while leaving the native label untouched in 24-hour mode.
Moved the rest clock moon and sun inside the dial beneath the midnight and noon labels without changing its hand, selection, or rest behavior.
Centered the configured dusk presentation fade on nightfall, beginning 30 seconds before and finishing 30 seconds after the phase boundary at the default 60-second duration; the existing pre-dawn fade still finishes exactly at dawn.
Added PurpleWyrdnessBrightness with a 1.2 default and 0.5-to-2.0 range for purple sky emission and HDR moon/world-light colors without taking ownership of exposure, post-exposure, light intensity, volumetrics, or Native Orange.
Added a fixed 10-active-second loaded-threat color ramp so restored threat no longer causes an abrupt red shift on the first stable rendered frame; authoritative threat, meter fill, gameplay, and night timing remain immediate.
Suppressed Eyes diagnostic GFT output at the title screen, during loading, and while no playable Hero exists while retaining GFT-owned compatibility notices.
Reduced the default BattlecryResponseCooldownSeconds from 45 to 15 active seconds while preserving the existing Battlecry Voice Tuner API, diminishing threat, randomized response pool, and two-to-three-cry cadence.
Incremented ConfigSchemaVersion from 10 to 11 because the existing BattlecryResponseCooldownSeconds default changed from 45 to 15 seconds. Genuine custom values remain eligible for conservative recovery.

Version 1.0.5
Added a versioned optional Battlecry Voice Tuner API that accepts battlecries only while the Hero is exposed outdoors during an active Wyrdnight.
Made repeated cries grant full, half, quarter, and then diminishing Wyrd Threat down to a 10 percent floor. Thirty active real-time seconds without another accepted cry restores the full gain.
Added seven randomized Wyrdnight responses after two or three accepted cries for the Atmospheric and Detailed Grail Floating Text presets. Responses use a separate 45-active-second configurable cooldown; Minimal remains focused on official hunts.
Added BattlecryResponseCooldownSeconds without changing ConfigSchemaVersion 10 because the setting is additive and existing configurations can safely receive its default.

Version 1.0.4
Applied the exposed-Wyrdnight restriction to the actual fireplace REST button after native initialization, after upgraded-bonfire action refreshes, and while the menu remains open. The entry now uses native greyed-out styling and cannot open the clock; the silent final action guard remains as a failsafe.
Simplified the rest clock to neutral presentation with the moon fixed at the top and sun at the bottom. Removed the Wyrdnight caption, purple arc, glow, and endpoint markers.
Added RestClockLabelFormat with 12-hour labels as the default and an optional 24-hour label set. The native clock hand, input, rest calculations, and interruption behavior remain unchanged.
Kept Wyrdnight lighting active while the rest popup is open and held the last confirmed presentation through short loading and transition states. Newly available night scenes are primed from the authoritative world clock so the game cannot briefly render vanilla lighting before Eyes' normal state poll.
Kept ConfigSchemaVersion at 10 because RestClockLabelFormat is additive and existing configurations can safely receive its default.

Version 1.0.3
Moved the 60-second dawn presentation fade into the final 60 active real-time seconds before dawn, using current world-clock progress and weather rate so the Wyrdnight palette reaches its restored state at the phase boundary instead of beginning there.
Added modest combat-window threat when the Hero releases a projectile or successfully completes a spell cast, even when it misses. Canceled and failed casts add nothing, projectile spells are not double-counted, and melee still requires damage or a confirmed environment impact.
Kept the presentation change isolated from gameplay phase, threat reset, meter visibility, protection, and world-clock switching. Config schema remains 10 because no setting changed meaning.

Version 1.0.2
Added AllowUnprotectedWyrdnightRest, disabled by default. Protected Wyrdnight rest remains available, while exposed REST controls use the game's native greyed-out and inactive presentation unless the option is enabled.
Removed the blocked-rest warning panel and its message-queue state. The original Rest action check remains as a silent failsafe for stale UI or alternate entry routes.
Kept daylight and all native rest denials authoritative, and preserved the new durable option across future compatible config recovery without changing schema 10.

Version 1.0.1
Reworked the native rest clock into an explicit 24-hour presentation without changing its selection controls: midnight is labeled at the top, 06 at the right, noon at the bottom, and 18 at the left.
Added a palette-matched Wyrdnight arc for the actual 22:05-to-05:31 world-clock phase, moved the moon and sun markers to those boundaries, and replaced the misleading generic 12/12 half-circle with an exact phase display.
Made the rest-clock overlay inherit Purple Wyrdness or Native Orange and the configured threat-to-red shift at the moment the screen opens.
Kept the presentation isolated and fail-safe: hierarchy or rendering failures retain the fully usable native rest clock and produce only one warning per failure episode.

Version 1.0.0
Prevented exposed-Wyrdnight beds and fireplace rest controls from opening the clock screen by filtering the native rest-availability result without overwriting it. Protected rest remains available and the original Rest action check remains as a failsafe.
Changed the blocked-rest notice to "You can rest during a Wyrdnight only within a protective boundary." and limited it to one notice per blocked episode so repeated interaction cannot queue messages.
Added WyrdVisualTransitionSeconds, default 60 active real-time seconds, to smoothly fade the integrated environment and protection-bubble palette across natural dusk and dawn. Wyrdnight gameplay, world-clock switching, meter visibility, and protection remain exact; loading, interiors, disablement, teardown, and failures restore immediately.

Version 0.9.9
Restored the full display, assembly, Nexus, and release ZIP title to Eyes in the Dark - Wyrdnight Overhaul. The internal package folder, DLL, plugin GUID, config title, and config filename remain unchanged.

Version 0.9.8
Replaced raw day/night timescale multipliers with DayMinutes, BaseNightMinutes, and MaximumThreatNightMinutes. Defaults now present as a 60-minute day, a 5-minute zero-threat night, and a 15-minute maximum-threat night.
Made Wyrdnight duration interpolate dynamically with current threat, so remaining unnoticed lets the night pass quickly while escalating threat draws it out. Clock writes are thresholded to avoid setter spam, and gameplay Time.timeScale remains untouched.
Kept capped square-root danger-budget scaling based on the configured maximum-threat night so a stretched default night retains roughly the prior 19 percent Watchful budget bonus.
Routed every Eyes atmospheric notification through GFT's Purple or Orange color group according to WyrdnessPalette while retaining independent priority and Wyrd icon behavior.
Added diagnostic EnableThreatOverride and ThreatOverrideValue settings. The override freezes natural gain and relief during a valid Wyrdnight while driving the meter, visuals, dynamic night duration, stalkers, and official hunts; dawn still resets threat.
Advanced ConfigSchemaVersion to 10 because DayTimescale and NightTimescale were removed and replaced by real-minute duration settings with dynamic threat semantics. Other compatible durable settings are backed up and conservatively restored; diagnostic override controls always retain safe new defaults during schema recovery.

Version 0.9.7
Changed the Wyrdnight sky color layer from the material's narrow _NightSkyTint property to its full _SkyTint property so the configured hue affects the complete visible sky.
Corrected config and documentation wording: the sky tint does not directly own fog, clouds, terrain lighting, or reflections and remains excluded from threat-driven red shifting.
Confirmed by decompilation that Light Control changes light intensity, volumetrics, exposure, and post-exposure only; it does not write either sky tint property.
Advanced ConfigSchemaVersion to 9 because NightSkyAmbientColor now controls the full visible-sky tint rather than the narrower night-texture tint. Compatible customized color and strength values remain eligible for conservative restoration.

Version 0.9.6
Integrated Purple Moon Test into Eyes in the Dark, including its moon disc, HDR corona, directional and volumetric moonlight, night-sky tint, and fueled protection-bubble body and border controls; promoted the tested live purple values to the new defaults.
Added Purple Wyrdness and Native Orange palettes. Native Orange derives low-threat hues from each region's game-owned values instead of replacing them with a hard-coded orange.
Replaced the boundary-only threat response with one configurable 0.8-to-1.2 visual-strength scale shared by the environment, protection bubble, boundary, and threat meter.
Added a configurable smooth shift toward #FF3028 for the moon, moonlight, protection bubble, boundary, and threat meter as threat rises. Night-sky color deliberately retains its selected base hue.
Made the threat meter 1.5 times brighter and applied the shared red shift while retaining its existing fill behavior, ownership, visibility, and layout contract.
Removed the old boundary threat toggle, boundary-specific minimum/maximum intensity settings, and threat-driven thickness escalation; ring geometry and organic pulses remain independently configurable.
Advanced ConfigSchemaVersion to 8 because the former boundary-only threat settings were removed and replaced by the shared Wyrd visual response. Compatible durable settings are backed up and conservatively restored; the retired standalone Purple Moon Test config is not migrated.
Retired the standalone Purple Moon Test package after its visual behavior and current settings were integrated.

Version 0.9.5
Promoted the tested threat-meter color and layered-boundary radii, brightness, thickness, minimum threat intensity, and pulse amount to the shipped defaults; Diagnostics remains off by default.
Advanced ConfigSchemaVersion to 7 because the threat-meter and boundary presentation defaults materially changed. The stale config is backed up and regenerated before compatible durable customizations are restored.
Kept the new BoundaryPulseAmount default of 0.8 and Diagnostics default of false during schema-7 recovery even when those two values were customized previously; other compatible customized settings remain eligible for conservative restoration.

Version 0.9.4
Made a fleeing stalker turn hostile when the pursuing Hero closes within 8 meters, while retaining zero hunt-budget spending and zero official-hunter relief.
Raised the passive observation buffer to 20 meters and added a five-active-second flee rearm delay so stalkers shadow from safer range without repeatedly restarting flee behavior.
Restricted Rest during an already-active Wyrdnight to native safe rest points inside fueled protective boundaries; daytime rest and native sleep-interruption checks remain unchanged.
Suppressed atmospheric GFT transitions during allowed sleep and reconciled directly to the final waking phase, preventing Wyrdnight begin/end text for transitions the Hero slept through.

Version 0.9.3
Restricted one-time provocation threat to attacks against an exact passive stalker; a stalker that already became hostile from its hidden threshold no longer grants the special bonus on a later hit.

Version 0.9.2
Added a separate ambient-stalker director and volatile runtime that share one encounter lane with official hunts without spending hunt budget or granting official-hunter relief.
Added 33 reviewed stalker profiles: 26 ordinary map-native creatures below 50 threat and seven opt-in high-pressure Sharg, Lost Knight, Finbled Heavy, and Drowned Knight profiles from 50 to below 75 threat.
Added hidden per-instance hostility thresholds of 45-55 for ordinary stalkers and 70-80 for high-pressure stalkers; an exact Hero attack escalates immediately and adds configurable one-time provocation threat.
Added native Observe, FollowMovement, and Flee transitions so passive stalkers watch, shadow, and retreat from deliberate pursuit while an owned combat block prevents premature aggression.
Required native walkable placement, graph connectivity, Wyrd-protection rejection, and an expanded off-camera margin before spawning; passive cleanup requires both distance and continuous camera absence, while hostile stalkers never release or disappear because of distance.
Added randomized threat-adaptive cooldowns, additive in-game pacing and placement settings, preset tuning, session failure rejection, concise diagnostics, and randomized stalker atmosphere pools.
Expanded Grail Floating Text routing: Minimal remains official-hunt-only, Atmospheric adds only witnessed disappearances, and Detailed adds sightings, retreats, and escalation flavor without exposing hidden aggression values.
Kept ConfigSchemaVersion at 6 because all ambient-stalker settings are additive and existing configurations can safely receive their defaults.

Version 0.9.1
Doubled confirmed scenery and non-damageable-object impact threat from 25% to 50% of CombatThreatPerWindow, producing 1 threat per accepted impact at the default cap while retaining one contribution per attack.
Added CombatResponseSeconds with a 1.5-second default, replacing the hard-coded 3-second combat aggregation delay while retaining the combined combat-window cap and pause-aware active-time behavior.

Version 0.9.0
Expanded the curated director to 50 reviewed profiles using shipped location specs, direct open-world scene references, and Steel and Bone NPC-template data for level, durability, family, and enemy-type review.
Replaced the speculative generic Sarras Wyrdspawn pool with native Sarras Drowners, Drowned crew, Finbled roles, Tadpole, Wailcap, Tidewraith, and Drowned Knights; Horns, Cuanacht, and Forlorn also gained broader native regional variety.
Added Allow Elite Enemies. Reviewed elites remain ineligible until Wyrd Threat is strictly greater than 75 percent, can never be sidecars, and are disabled by Uneasy and Watchful while Cursed enables them.
Reclassified the Horns Sharg through its shipped Elite actor data and added reviewed Forlorn and Sarras elites while continuing to reject bosses, minibosses, friendlies, summons, story, challenge, trial, custom, arena, and hero-summon variants.
Adjusted several player-level gates and danger costs to reflect the resolved NPC template rather than relying on the location-spec tier name alone.
Kept ConfigSchemaVersion at 6 because AllowEliteEnemies is additive; existing configs receive its safe false default.

Version 0.8.9
Added OuterRingIntensityMultiplier with a 1.0 default so the near, middle, and outer rings all expose the same explicit brightness control beneath the shared BoundaryHdrIntensity master.

Version 0.8.8
Expanded BoundaryPulseAmount from 0-0.5 to 0-1.0 so each independently timed ring can be tuned from a restrained ebb to a full dim-to-double-brightness pulse; the default remains 0.12.

Version 0.8.7
Added an EITD-owned three-ring Wyrd boundary with independently configurable near, middle, and outer radii, brightness, and thickness; disabling layered mode restores the native single-edge presentation.
Added smooth bounded per-ring pulse animation and subtle Wyrd Threat response for brightness and thickness without changing any ring radius, native mask intensity, protection, or gameplay detection.
Reorganized FoA Mod Manager into focused threat, hunt, HUD, boundary, notification, and diagnostic groups while retaining every existing stored section and key.
Advanced ConfigSchemaVersion to 6 because the default boundary threat response changed from Disabled to Subtle; the stale config is backed up and regenerated before compatible customized values are restored.

Version 0.8.6
Added Wyrd Threat from confirmed melee impacts against scenery and non-damageable objects, limited to one contribution per attack and routed through the existing short combat aggregation cap; empty swings add nothing.
Added the live ThreatMeterColor setting with a #B878FF default and safe fallback for invalid HTML RGB values.

Version 0.8.5
Added an internal +9 horizontal and -9 vertical baseline to standalone vanilla-HUD threat-meter placement while keeping both user-facing offset defaults at zero.
Kept Glorious UI placement free of the standalone baseline so its below-resource-bars contract remains unchanged.
Advanced ConfigSchemaVersion to 5 because MeterOffsetX and MeterOffsetY now adjust from an internal baseline; old direct-offset values would otherwise be applied twice. The reset keeps the new zero adjustment defaults for both settings while preserving other compatible customized values.

Version 0.8.4
Renamed the full mod and export title to Eyes in the Dark - Wyrdnight Encounters so generated release ZIPs use the new name.
Standardized Wyrdnight as one word throughout player-facing text, configuration descriptions, diagnostics, tests, and documentation.

Version 0.8.3
Added first-class dynamic world-clock control with separate configurable day and night weather multipliers, defaulting to approximately 60 minutes of daylight and 15 minutes of night without touching gameplay Time.timeScale.
Expanded the curated native roster across Cuanacht, Forlorn, and Sarras with exact player-level gates, danger costs, one-copy limits, family repetition control, sidecar roles, and solo-primary restrictions; unknown scenes and empty pools still fail closed.
Required every placed encounter member to confirm native combat with the exact Hero before budget is spent, and added bounded same-scene outdoor reacquisition every two active seconds with a three-attempt per-member cap.
Added 30-second unscaled listener retry backoff with one warning per failure episode, and aggregated passive-exposure and decay diagnostics into ten-active-second summaries.
Removed the unused regional progression-tier abstraction; player level, region, template safety, session failures, and danger budget now form the complete hard eligibility model.
Added GFT-owned incompatibility reporting for Custom Timescale because both mods control GameRealTime weather rates; Eyes does not disable or reconfigure either plugin.
Kept ConfigSchemaVersion at 4 because the three world-timescale settings are additive and previous configs can accept their current defaults safely.

Version 0.8.2
Folded the standalone Purple Wyrdness presentation into Eyes in the Dark and removed the retired standalone repository package.
Changed the default boundary to the purple #B878FF hue at the shipped edge's peak HDR brightness while restoring the vanilla radius of 32, thickness of 0.25, and static reactivity behavior; every retained visual remains configurable in FoA Mod Manager.
Incremented ConfigSchemaVersion to 4 because boundary HDR intensity, radius, thickness, and threat-reactivity defaults changed materially; the old config is backed up and regenerated before compatible customized settings are restored.

Version 0.8.1
Authorized Glorious UI's new plugin GUID for the Eyes-owned Wyrd Threat placement contract and refreshed the integration test identity.

Version 0.8.0
Finalized the initial beta roster from shipped scene evidence: Wyrdspirit remains the only reviewed universal candidate, while Redcap, Corpse Eater, Sharg, and Ogre stay restricted to Horns of the South.
Finalized initial level gates, danger costs, pack ceilings, hazard curves, threat tuning, relief, warning, recovery, and the Uneasy Night, Watchful Night, and Cursed Night templates for consolidated in-game validation.
Finalized randomized Minimal, Atmospheric, and Detailed GFT routing plus concise diagnostics-only System summaries for state, threat, pacing, selection filters and weights, composition, cost, placement, and resolution.
Hardened missing-hero, unknown-region, invalid-template, failed-member, lost-primary, loading, travel, portal, death, dawn, and gameplay-load handling around one atomic official encounter.
Added installed-user compatibility, beta-limit, and troubleshooting documentation plus complete Nexus page metadata for the feature-complete candidate.
Marked KS Wyrd Hunt Addon as retired in repository and Nexus documentation; Eyes in the Dark contains no runtime migration or compatibility layer for it.

Version 0.7.0
Finished the versioned Eyes-owned HUD placement contract: Glorious UI only requests below-bars placement, and a missing, disabled, or failed bridge leaves the meter in its standalone row above health.
Verified that Glorious UI contains no active duplicate Wyrd meter implementation or Wyrd Hunt meter dependency.
Isolated threat-meter and native-boundary presentation failures so they disable only the failed optional presentation path, log once, and leave threat and encounters active; GFT already follows the same isolation rule.
Reduced periodic presentation work by caching parsed boundary colors and updating exact threat text only when its rounded value changes.
Finalized config descriptions, ranges, schema-reset reason, recovery wiring, and the one-shot preset permanent exclusion for the 0.8.0 candidate cycle.

Version 0.6.0
Added a curated regional director with exact Horns of the South, Cuanacht, Forlorn, and Sarras scene handling; unknown scenes fail closed, the shipped scene data keeps Redcap, Corpse Eater, Sharg, and Ogre profiles in Horns of the South, and only the reviewed Wyrdspirit profile is universal.
Added player-level and regional-progression gates, per-profile danger costs, threat-driven weighted primary selection without hard threat thresholds, recent profile/family penalties, and session rejection after three failures from one template.
Added atomic mixed encounters with weaker-sidecar preference, same-tier and same-family penalties, level/preset/budget pack caps, and curated Wyrdspirit clusters up to three members.
Made the exact primary actor resolve the official hunt while surviving sidecars become ordinary enemies and release the director lock.
Added one-shot Uneasy Night, Watchful Night, and Cursed Night gameplay presets that return to Custom and preserve HUD, GFT, boundary, and diagnostic preferences.
Expanded diagnostics and optional GFT System summaries with concise eligibility filters, final weights, composition, danger cost, budget, placement, and outcome information.
Incremented ConfigSchemaVersion to 3 because FirstHunterDangerCost was replaced by per-profile costs plus DangerCostMultiplier and FirstHunterSpawnDistanceMeters was renamed to HunterSpawnDistanceMeters; the old config is backed up and regenerated before approved durable settings are restored.

Version 0.5.0
Added accumulated encounter hazard with randomized targets, threat and night-progress weighting, remaining-budget gating, warning commitment, and recovery without fixed Wyrd Threat spawn thresholds.
Added one reviewed solo Wyrdspirit official hunter using native navigation placement, exact volatile Location/Npc ownership, hostile and combat-capable validation, native combat entry, and non-persistent runtime state.
Added protected-area, pacifist-safe-zone, unrelated-combat, swimming, travel, loading, scene, and placement gates. Invalid or failed placement spends no danger budget.
Added exact official-hunter death, sustained outdoor escape, interior escape, dawn, death, gameplay-load, and lost-target resolution. Kills grant greater threat relief and shorter recovery than escapes.
Added randomized GFT hunt commitment, hunter-killed, and escape pools on the hunt collapse lane, with Warning presentation for commitment and concise diagnostics-only System summaries for placement, budget, and resolution decisions.
Added the GFT-owned soft main-menu System notice when Wyrd Hunt and Eyes in the Dark are loaded together; Eyes itself contains no compatibility scanner or automatic handling.

Version 0.4.0
Added configurable native Wyrd boundary color, HDR intensity, visual radius, thickness, and optional subtle threat reactivity without changing protection, mask intensity, or gameplay radius.
Added optional Grail Floating Text notifications with Minimal, Atmospheric, and Detailed routing, randomized atmospheric text pools, immediate-repeat prevention, shared Wyrd icon use, and pause-aware lane cooldowns.
Added concise diagnostics-only GFT System summaries for meaningful state, threat-source, pacing, and optional-integration events, with immediate delivery, repeat suppression, and failure isolation.
Added a world-clock-aware nightly danger-budget calculation with a capped square-root long-night bonus. This milestone reports the budget but does not spend it or spawn enemies.

Version 0.3.3
Shortened the BepInEx and FoA Mod Manager config title to Eyes in the Dark while retaining the full package name.
Mirrored the Eyes-owned Wyrd Threat meter artwork horizontally and vertically using the proven fill-origin, glow-indicator, and anchor handling previously used by Glorious UI.
Added the versioned Eyes HUD placement contract so Glorious UI can request a below-bars layout without creating, updating, hiding, or destroying the meter.

Version 0.3.2
Moved the standalone Wyrd Threat meter from the overlapping health-bar slot to its own row above the vanilla health bar.
Reduced the default protected decay from 10 to 4 threat per active real-time minute and interior decay from 3 to 1 so extended-timescale nights retain readable pressure. Config schema 2 resets these materially changed untouched defaults while preserving compatible customized settings.

Version 0.3.1
Preserved current-night threat through ordinary scene loading and interior transitions by limiting reconstruction resets to the game's native gameplay-load entry points.
Prevented portal, fast-travel, loading, and scene-transition progress jumps from adding catch-up passive threat.

Version 0.3.0
Added continuous 0-100 Wyrd Threat with Unnoticed, Watched, Hunted, and Marked presentation stages.
Added normalized-night-progress passive threat that reaches the same baseline at vanilla and slower world timescales.
Added throttled threat from sustained sprinting or fast swimming, meaningful combat, eligible Wyrd kills, direct pickups, and container or corpse looting.
Added moderate protected-area decay, slower active-real-time interior decay, dawn reset, modest load reconstruction, and load or interior-exit grace.
Added an Eyes-owned purple Wyrd Threat meter above the vanilla Hero HUD, including protected-night visibility, automatic hiding, optional exact values, and offsets.
Added the first config schema and shared previous-settings recovery infrastructure.

Version 0.2.4
Removed the temporary high-volume transition checkpoint tracing after it confirmed every sampled Eyes state read completed safely.
Travel now takes diagnostic priority over the overlapping generic transition state so portal and fast-travel suppression can be verified directly.

Version 0.2.3
Added short-lived numbered diagnostics around every Eyes world-state read during loading and scene transitions.
Each diagnostic sample now records a final completion checkpoint so a hard crash can be separated from an interrupted Eyes state read.

Version 0.2.2
Fixed an early title-screen state sample that could initialize the game's scene-lifetime event service before its world event system existed and prevent startup.
The sampler now waits for the game-owned Wyrdness service, playable hero, and active scene before reading scene-lifetime state.

Version 0.2.1
Added pause and unpause transition diagnostics so stopped active-real-time behavior can be verified in the game log.

Version 0.2.0
Added fail-closed detection for playable hero, outdoor scenes, Wyrdnight, protection, loading, transitions, travel, rest, and death.
Added normalized Wyrdnight progress from the game's world clock.
Added an active-real-time clock that stops during pauses and suppressed gameplay states and rejects long catch-up frames.
Added low-noise diagnostics that log only meaningful state, scene, or exposure changes.

Version 0.1.0
Added the initial Eyes in the Dark BepInEx plugin and package structure.
Added the living design document for threat, pacing, encounters, HUD, Wyrd boundary visuals, integrations, and compatibility boundaries.
Kept the development scaffold gameplay-inert while the design is established.

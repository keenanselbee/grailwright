Version 0.9.2
Added Execution Move Toward Vanilla and Execution Shoulder Retraction controls
under Advanced - Animation Guards. Executions now smoothly reach the full
vanilla presentation offset by default while retaining the existing 0.12 metre
minimum shoulder/chest retraction and restoration timing.
Kept ConfigSchemaVersion at 20 because both settings are additive and existing
settings retain their defaults, types, sections, and meanings.

Version 0.9.1
Required active melee attack guards to be backed by their FSM's current
non-magic item, preventing a stale one-handed heavy-attack state from halving
FPAA offsets while Blood or Life Transfusion is equipped. Evaluating ownership
per FSM preserves valid melee guards in mixed magic/melee loadouts.
Kept ConfigSchemaVersion at 20 because this changes runtime state filtering
without changing a setting, default, type, section, or meaning.

Version 0.9.0
Added focused diagnostics for the remaining post-cast offset issue. Diagnostics
now identify each active FSM holding the sheathing guard, including its layer,
current and pending states, normalized time, and retained blend, and report the
configured versus effective local offset while any animation guard is active.
Kept ConfigSchemaVersion at 20 because this adds diagnostics without changing a
setting, default, type, section, or meaning.

Version 0.8.9
Ignored inactive animator FSM layers when resolving the shared sheathing guard,
preventing stale magic or equipment unequip states from holding first-person
arms at vanilla offsets until the next weapon change.
Kept ConfigSchemaVersion at 20 because no setting, default, type, section, or
meaning changed.

Version 0.8.8
Added soft Killing Blow Mastery execution-visual API integration under the existing animation-guard master switch.
Smoothly moves active Executions halfway toward vanilla and raises shoulder/chest retraction to at least 0.12 metres, composing by strongest influence with existing guards.
Restores over 0.25 seconds when the Execution completes and includes an exact target-death/native-state grace fallback so stale external lifecycle state cannot hold the guarded pose indefinitely.
Kept ConfigSchemaVersion at 20 because the integration adds no setting or default.

Version 0.8.6
Changed the default Bow Draw Maximum Offset from 67 to 33 percent, so the default 0.30 metre Bow Depth Offset contributes about 0.10 metres while drawing, matching the validated setting.
Advanced ConfigSchemaVersion from 19 to 20 because the existing bow-draw ceiling default changed. The stale config is backed up and regenerated; an untouched old 67 percent default adopts 33 percent, while compatible customized values remain eligible for recovery.
Cleared every reusable Kandra, Drake, weapon-entity, and attached-effect scratch buffer during scene-cache teardown so old scene component wrappers are released promptly without discarding reusable capacity or changing runtime behavior.

Version 0.8.5
Reused Kandra culling, Drake entity tracking, weapon entity-access, and attached-effect discovery buffers instead of recreating their managed collections during render synchronization and periodic hierarchy refreshes.
Switched recurring component discovery to Unity's list-based overloads, reused attached-effect state storage, hoisted each Drake access transform conversion out of its entity loop, and avoided a redundant ECS dependency completion after the same update already completed it. Refresh timing, job ordering, diagnostics, and visible behavior remain unchanged.

Version 0.8.4
Gave the bow guard priority over melee attack guards throughout pull, hold, release, and the gradual post-shot restoration, preventing overlapping one-handed attack states from masking the restored bow offset.
Excluded the already-smoothed melee correction from final offset composition while bow priority is active, so residual attack-guard blending cannot briefly pull the restored bow position back toward vanilla.
Required melee FSM layers to be active before their current or pending attack states can trigger either attack-detection path, preventing inactive stale layers from influencing first-person presentation.
ConfigSchemaVersion remains 19 because this corrects guard arbitration without changing any setting, default, type, section, or meaning.

Version 0.8.3
Changed dodge activation from unreliable frame polling to the native successful-dash callbacks plus the active DashBaseState update, while retaining FSM enumeration as a compatibility fallback.
Accelerated dodge retraction to a 0.06-second ease-out toward 0.25 metres so torso correction begins before the dodge pose can expose Cloth2, while retaining the chained-dodge hold and 0.20-second restoration.
Anchored bow restoration to BowRelease's native projectile-fire threshold at normalized time 0.05 instead of retaining the draw ceiling through the complete release animation.
Extended bow restoration from 0.22 to 0.40 seconds so it begins just after the arrow fires but returns to the configured offset more gradually; cancelled draws use the same gradual restoration.
ConfigSchemaVersion remains 19 because these changes only correct internal guard detection and transition timing without changing any setting, default, type, section, or meaning.

Version 0.8.2
Replaced dodge movement toward vanilla with dynamic shoulder retraction: every directional dodge now eases from the configured retraction toward 0.25 metres, holds maximum correction across rapid chained dodges, and smoothly restores without shifting the complete viewmodel offset.
Smoothed attack and sprint-attack guard transitions and made bow guarding follow normalized pull progress during arrow nocking before holding its ceiling through the held and release phases and easing out after release or cancellation.
Changed the default bow depth to 0.30 metres and the default bow-draw ceiling to 67 percent, producing about 0.20 metres of positive FPAA depth during draw.
Changed the default Shoulder Retraction from 0.25 to 0.05 metres so normal poses use a lighter correction while the dodge guard can dynamically reach the supported 0.25 metre maximum.
Consolidated the raw attack guard and held-melee tuning settings into Advanced - Animation Guards and removed the obsolete DodgeMoveTowardVanillaPercent setting and Advanced - Dodge Guard section.
Advanced ConfigSchemaVersion from 18 to 19 because bow and shoulder defaults changed, guard settings moved sections, and the retired dodge-offset setting was removed; compatible customized settings with unchanged current section and key names remain recoverable.

Version 0.8.1
Expanded shared attack guarding across every normal melee light and heavy phase, including initial, chained, tired, forward, charged, held, release, and alternate states, while retaining the narrower established behavior when shared targeting is disabled.
Expanded sheathing guarding from active two-handed melee grips to normal and alternate unequip states across melee, dual-wield, bow, and magic equipment.
Changed simultaneous shared guards to use the strongest move-toward-vanilla influence instead of multiplying independent retained-offset factors, preventing overlapping states from over-correcting the viewmodel.
Added an enabled-by-default bow-draw guard for pull, hold, release, and cancelled draw with a live 0-100 percent maximum tied dynamically to BowForwardOffset; the default 100 percent keeps a 0.10 bow depth at a 0.10 metre ceiling without changing animation, aim, or projectile origin.
Adopted the validated player configuration as the new defaults: 0.25 Shoulder Retraction; 0/0/100/100/100/30/20 standard retraction profile; 50 percent torso-renderer retraction; shared guard targeting at 50 percent; 75 percent head-bob speed; full stabilized viewmodel follow; and temporal-safe timing enabled. Diagnostics remains disabled by default.
Advanced ConfigSchemaVersion from 17 to 18 because these validated presentation, retraction, guard, and head-bob defaults materially replace the previous defaults; compatible customized settings remain recoverable.

Version 0.7.9
Added an Advanced - Animation Guards section with a master switch and independent attack, dodge, and two-handed sheathing toggles, all enabled by default to preserve current behavior.
Added an off-by-default shared 0-100 percent Move Toward Vanilla target for enabled animation guards; 0 retains the configured FPAA position and 100 reaches vanilla.
Kept the established held-heavy, sprint-attack, dodge, and sheathing targets unchanged while shared targeting is off, and made shared mode override held-melee retained-scale and extra corrections so every selected guard reaches the same exact target.
Moved only the attack toggle's FoA Mod Manager presentation into the consolidated section while retaining its existing raw config key and section for compatibility.
ConfigSchemaVersion remains 17 because all new controls are additive with compatibility-preserving defaults and the attack control change is display metadata only.

Version 0.7.8
Replaced the temporary renderer-isolation selector with a permanent Torso Renderer Retraction control after diagnostics confirmed that the obstruction belongs to the second native Cloth renderer, whose mesh and material identify it as torso geometry.
Added a live 0-400 percent renderer-specific weight, defaulting to 100 percent of the master Shoulder Retraction distance, with semantic Torso matching and Cloth2 as a compatibility fallback.
Assigned only the matched torso renderer to a dedicated render rig so its complete skinned surface can retract without moving the first-person arms, hands, weapons, or other cloth renderers, while retaining the existing bone job and per-renderer culling alignment.
Advanced ConfigSchemaVersion from 16 to 17 because RendererIsolationTarget was removed after completing its diagnostic purpose; compatible durable settings remain recoverable and the new torso-renderer weight starts at its current default.

Version 0.7.6
Kept missing or independently disabled renderer-isolation targets in a stable observed state so the half-second discovery refresh does not repeat identical missing/restored diagnostics while still detecting a renderer that later appears or becomes enabled.
ConfigSchemaVersion remains 16 because renderer-isolation choices and all existing settings retain their 0.7.5 meanings and defaults.

Version 0.7.5
Added an off-by-default Renderer Isolation Target diagnostic with None, FppBody, Cloth1, Cloth2, and Cloth3 choices for identifying native first-person geometry that does not respond to bone retraction.
Temporarily unregisters only the selected Kandra renderer, restores it before switching targets, and automatically restores FPAA-owned isolation when the mod is disabled, third person starts, the scene changes, or the plugin unloads.
Logged the selected renderer's full hierarchy path, Kandra mesh, materials, and stable body/cloth candidate ordinals so identical Cloth objects and dynamically added third cloth renderers can be distinguished.
ConfigSchemaVersion remains 16 because the new diagnostic selector is independently bound, defaults to None, and does not alter existing settings or behavior.

Version 0.7.4
Added off-by-default live 0-400 percent grouped controls for the native Hips lower-torso bone, four left/right chest-helper bones, two shoulder-fix helpers, and the complete Cloth_Skirt group discovered on the FppArms rig.
Applied grouped correction additively with the existing standard profile and exact-name test target inside the same single Kandra bone job, leaving hands and weapon sockets unchanged unless explicitly selected by the diagnostic target.
Guarded native-cloth correction behind a verified contiguous Cloth_Skirt index range so an unfamiliar rig layout cannot sweep unrelated bones, and expanded Diagnostics with resolved group indices, counts, contiguity, and effective metres.
ConfigSchemaVersion remains 16 because all four grouped controls are additive and disabled by default, preserving existing behavior and settings.

Version 0.7.3
Added an off-by-default exact-name Test Retraction Bone target and a live 0-400 percent additive test control for identifying native first-person mesh regions weighted outside the standard spine, shoulder, and arm profile.
Expanded Diagnostics with the complete indexed bone inventory for every native first-person Kandra rig, the selected test bone and resolved index, and every discovered renderer path with its rig name.
Kept exploratory retraction inside the existing single Kandra bone job, cache-rebuilding immediately when the test name changes, with no root transform mutation, additional render pass, or automatic hand movement.
ConfigSchemaVersion remains 16 because the diagnostic target settings are additive, disabled by default, and do not alter existing behavior.

Version 0.7.2
Added live 0-200 percent retraction-profile controls for Spine, Spine1, Spine2, left shoulder, right shoulder, upper arms, and forearms, with defaults matching the established 35/75/100/100/100/60/20 percent curve.
Allowed asymmetric shoulder testing and up to twice the master Shoulder Retraction distance per region while keeping both hands fixed at zero and retaining the existing single Kandra bone job.
Expanded Diagnostics with effective metres for every adjustable region and a change-triggered per-rig report of all matched spine, shoulder, upper-arm, and forearm bone indices.
ConfigSchemaVersion remains 16 because these are additive live controls whose defaults reproduce the existing retraction profile.

Version 0.7.1
Extended Shoulder Retraction from 0.10 to 0.25 metres and removed the active equipment-depth cap, allowing the rendered torso to move behind its vanilla position when attack poses expose body geometry.
Added the lower Spine bone at 35 percent, strengthened Spine1 from 35 to 75 percent, and retained full Spine2 and shoulder movement with the existing upper-arm and forearm taper to zero at the hands.
Kept the correction inside the existing single Kandra bone job and steadily active through attack, dodge, sheathing, and interaction guards, with no additional render pass or gameplay-transform changes.
Advanced ConfigSchemaVersion from 15 to 16 because Shoulder Retraction now has a stronger uncapped meaning; legacy customized values are not automatically restored, while other compatible durable settings remain recoverable.

Version 0.7.0
Added an off-by-default Shoulder Retraction setting from 0.00 to 0.10 metres for reducing upper-body and shoulder visibility introduced by positive first-person depth offsets; start around 0.02 to 0.04.
Applied the correction after animation inside the existing Kandra bone job, with fixed tapering from Spine1 through Spine2 and the shoulders, upper arms, and forearms to zero at the hands so weapon sockets, aim, gameplay transforms, physics, projectiles, and third person remain unchanged.
Kept shoulder retraction steadily active while the established heavy-attack, sprint-attack, dodge, sheathing, and interaction guards adjust the complete presentation; the correction is independently capped by the active equipment category's positive forward offset.
Cached only managed per-rig shoulder bone indices, rebuilding when rig bone counts change and clearing them with the existing scene caches, so the feature adds no render pass, second bone job, persistent native allocation, or per-weapon work.
Added live Diagnostics reporting for configured and applied retraction, world offset, affected bone count, and affected rig count. ConfigSchemaVersion remains 15 because the new setting is an independently bound addition and existing settings keep their meanings and defaults.

Version 0.6.9
Added an off-by-default Stabilize Viewmodel During Head Bob toggle that forces arms, weapons, effects, culling, and visual integrations to follow FPAA's exact camera-space bob translation, overriding the partial follow percentage.
Separated configured positioning from head-bob compensation so normal offsets remain based on the first-person arms pivot while bob follow uses the camera's actual world-space vector, removing the rotational basis mismatch that could leave residual viewmodel movement at 100 percent.
Added a live Head Bob Speed setting from 50 to 150 percent, defaulting to 100, which scales cadence after the soft-knee limiter without changing amplitude, movement speed, sprint emphasis, temporal timing, or render cost.
Expanded diagnostics with the speed-scaled target cadence and camera, applied-stabilization, and remaining-bob world vectors. ConfigSchemaVersion remains 15 because both new settings are independently bound additions and existing settings keep their meanings and defaults.

Version 0.6.8
Added a live Viewmodel Head-Bob Follow setting from 0 to 100 percent, defaulting to 75 so arms and held visuals follow most of the camera translation while retaining one quarter of the apparent relative motion.
Applied the follow component through the existing shared post-camera-rotation visual offset so Kandra arms, body rendering, Drake equipment, attached effects, culling, and API integrations remain aligned.
Kept the follow render-only and behind the established head-bob, first-person, accessibility, and main-camera gates; aim, attacks, colliders, projectiles, camera motion, temporal timing, and third person remain unchanged.
Added follow percentage startup and runtime diagnostics. ConfigSchemaVersion remains 15 because adding this independently bound setting does not invalidate existing config values.

Version 0.6.7
Replaced the hard jog-cadence clamp with a continuous soft-knee curve that preserves speed and preset differences before approaching the same safe 3.2 walking and 4.2 maximum-sprint limits.
Changed Head Bob Smoothness to ease cadence and vertical/lateral amplitude transitions instead of filtering the completed waveform, preserving steady jogging strength and the intended two-step lateral-to-vertical gait at every smoothness value.
Added smoothed cadence and vertical/lateral amplitude values to Diagnostics and deterministic soft-limit contract coverage.
Advanced ConfigSchemaVersion from 14 to 15 because Head Bob Smoothness now controls gait response rather than final waveform filtering; regenerated defaults use the new behavior while compatible customized settings remain recoverable.
Kept temporal-safe HDRP timing, motion-blur suppression, presets, Sprint Emphasis, render scope, camera restoration, viewmodel positioning, and compatibility integrations unchanged.

Version 0.6.6
Kept the configured first-person arms, body, weapons, effects, and integration offsets active when the pause menu stops VHeroController.ProcessUpdate while HDRP continues rendering.
Added a pause-only render fallback that recalculates a missing current-frame offset from the live first-person arms pivot, retaining the authoritative post-camera-rotation snapshot throughout normal gameplay.
Added a rate-limited Diagnostics line when the paused-render fallback supplies the shared offset.

Version 0.6.5
Capped the distance-driven vertical head-bob cadence at a smoothly blended 3.2 cycles per second while walking and 4.2 while sprinting, preventing high movement speeds and Strong sprint settings from producing unnaturally rapid stepping.
Kept the existing preset amplitudes, lateral-to-vertical gait relationship, smoothing, sprint blending, temporal-safe timing, render scope, and restoration behavior unchanged.
Added raw and effective vertical cadence values to Diagnostics so movement-speed input and limiting can be confirmed independently.

Version 0.6.4
Added an off-by-default Temporal Safe Head Bob Timing A/B setting. When enabled, FPAA applies the unchanged main-camera bob immediately before HDRP records its current and previous temporal view matrices instead of at the later camera-render callback, then uses the existing end-of-render and next-update restoration safeguards. This targets TAA, DLSS, and FSR smearing without changing bob motion, viewmodel positioning, yaw stabilization, rendering quality, or render-pass count.
Added Diagnostics confirmation of the application phase and whether HDRP captured the exact bobbed camera position.

Version 0.6.3
Based the shared presentation offset on the first-person arms pivot immediately after the game applies current look input, eliminating the delayed rendered-camera direction that remained visible as jerky arms during rapid left-right turns.
Kept the existing once-per-frame synchronization hook, shared Kandra, Drake, effect, render, and API offset, and a guarded rendered-camera fallback without adding interpolation, viewmodel lag, or per-consumer work.

Version 0.6.2
Added an off-by-default Suppress Motion Blur During Head Bob setting for players who find whole-camera bob blurry with HDRP motion blur.
Excluded only the main first-person camera's movement from HDRP motion blur while visible FPAA bob is active, then restored its exact previous value after the render.
Kept moving-object blur, other cameras, other post-processing features, stationary frames, and the game's saved graphics settings unchanged.

Version 0.6.0
Captured the shared first-person presentation offset after the hero controller applies the current frame's camera rotation, preventing rapid left-right look reversals from rendering against a stale view direction.
Kept one immutable offset for Kandra arms, Drake equipment, culling, attached effects, the body render translation, and API consumers so the timing correction does not reintroduce hand, weapon, effect, or compatibility separation.
Retained the established rendered main-camera coordinate basis and made the current-frame snapshot authoritative before presentation and LateUpdate integrations consume it.

Version 0.5.9
Added a live DodgeMoveTowardVanillaPercent setting from 0 to 100, with a default of 50 so dodges retain half of the configured presentation offset at their midpoint.
Kept chained and redirected dodges continuous by starting each restarted dodge from the currently displayed offset instead of snapping back to the full configured position.
Held the last valid dodge blend through temporary animator loading gaps and eased interrupted dodges back to the configured position.

Version 0.5.8
Made the complete first-person presentation offset ease from its configured position to vanilla and back over every directional dodge animation.
Used the Legs FSM animation clock so the arms, equipment, and attached effects reach vanilla at the dodge midpoint and return to the configured setting before the dodge ends.

Version 0.5.6
Removed numeric prefixes from the generated config and aligned every raw section with the established FoA Mod Manager categories.
Advanced ConfigSchemaVersion from 13 to 14 because settings moved between raw sections; this one-time cleanup regenerates defaults instead of guessing mappings from the retired layout.
Kept Diagnostics penultimate and Import Previous Settings last.

Version 0.5.5
Removed the optional vanilla first-person head-bob multiplier and the related locomotion depth guard, eliminating the arm-moving camera-motion path and its four settings.
Consolidated camera motion into one Head Bob tab with Enable Head Bob, Head Bob Strength, Head Bob Smoothness, and Sprint Emphasis.
Kept the game's Accessibility / Head Bob setting as the global master switch while suppressing its native first-person arm movement and leaving third person untouched.
Renamed the four camera-only settings to match their final Head Bob role and retained the Balanced, 0.7 smoothness, and 0.75 sprint-emphasis defaults.
Incremented the config schema because the vanilla and locomotion settings were removed, the camera-only settings were renamed and moved, and Diagnostics moved to its final stored section; current defaults are regenerated while other compatible customized settings remain recoverable.

Version 0.5.4
Enforced the vanilla Accessibility / Head Bob gate again immediately before rendering alternate motion, preventing even its smoothed residual offset from appearing after the setting is turned off.

Version 0.5.3
Gated alternate camera motion behind the game's vanilla Accessibility / Head Bob setting, allowing that global accessibility control to disable both FPAA camera-motion paths.
Kept EnableVanillaHeadBob independent and off by default, so the accessibility setting can authorize alternate camera-only motion without enabling the arm-affecting native bob.
Incremented the config schema because EnableAlternateCameraMotion now requires the vanilla accessibility setting; compatible customized settings remain recoverable.

Version 0.5.2
Integrated Immersive Camera Motion's render-only first-person camera bob into First Person Arms Adjuster and retired the standalone package.
Enabled the alternate camera motion by default with Subtle, Balanced, and Strong intensity presets, adjustable path smoothing, and native sprint detection.
Raised the default sprint impact to 0.75, adding about 56% amplitude and 19% cadence while sprinting with the Balanced preset.
Kept the complete native head-bob multiplier and locomotion depth guard available under Camera Motion - Vanilla Head Bob, but added EnableVanillaHeadBob as an off-by-default master switch.
Separated FoA Mod Manager controls into Camera Motion - Vanilla Head Bob and Camera Motion - Alternate.
Incremented the config schema because the new vanilla head-bob switch changes first-person camera-motion ownership and defaults; compatible customized settings remain recoverable.

Version 0.5.1
Changed the locomotion depth guard's post-movement release from 1.00 second to 0.40 seconds, matching its engagement duration.

Version 0.5.0
Lengthened the locomotion depth guard's post-movement release from 0.55 seconds to 1.00 second for a gentler return to the configured first-person depth.

Version 0.4.9
Gated the locomotion depth guard behind the vanilla Accessibility / Head Bob toggle and FPAA's first-person head-bob multiplier, smoothly releasing the guard whenever either source disables first-person bob.
Incremented the config schema because MitigateLocomotionBodyIntrusion now applies only while first-person head bob is active; compatible customized settings remain recoverable.

Version 0.4.8
Changed the default first-person head-bob multiplier to 0.5 and the default locomotion depth retained to 0.75, matching the tested balanced camera-motion profile.
Moved both locomotion guard controls into the Camera Motion tab and clarified that the guard is only needed while first-person head bob is enabled.
Lengthened the smooth locomotion depth transition to 0.40 seconds entering movement and 0.55 seconds settling after movement, with speed hysteresis to prevent threshold flicker.
Incremented the config schema because HeadBobStrengthMultiplier and LocomotionDepthOffsetScale now have materially different defaults; customized compatible values remain recoverable.

Version 0.4.7
Added a smooth locomotion depth guard that reduces only the configured camera-space depth offset during grounded movement, preserving horizontal and vertical placement while keeping shoulder and torso geometry away from the camera.
Added a configurable first-person native head-bob multiplier with the vanilla accessibility toggle preserved and True Third Person's third-person value left untouched.
Made First Person Arms Adjuster the authoritative first-person head-bob owner; disable the separate Immersive Camera Motion plugin when using this version.

Version 0.4.6
Sampled one immutable camera-space presentation offset per frame for Kandra arms, Drake equipment, attached effects, and the temporary body render translation, keeping hands connected to weapons during rapid horizontal and vertical camera movement.

Version 0.4.5
Detected the game's dedicated LightAttackForward sprint-attack state for both one-handed and two-handed melee instead of treating the move as a heavy charge.
Rapidly returned the complete first-person presentation offset to vanilla during sprint attacks, then smoothly restored the configured position after the attack ended.
Expanded melee diagnostics to report held-heavy and sprint-attack mitigation independently.

Version 0.4.4
Removed the 0.4.3 VFPB whole-body overlay translation that could push its camera-anchored torso and legs into view.
Applied native first-person Kandra arm and culling offsets at Kandra's render-collection stage instead of waiting for the linked-weapon sync callback, keeping arms aligned across perspective order, equipment changes, and scene reloads while leaving VFPB's overlay placement untouched.

Version 0.4.3
Added automatic VFPB integration so its visible first-person body follows horizontal, vertical, and equipment depth offsets regardless of perspective, equipment, or scene-load order.
Restored VFPB's native overlay placement before its own updates and reapplied only the visual offset afterward, preserving head anchoring and preventing accumulated movement.

Version 0.4.2
Restricted the sheathing offset blend to melee weapons in their active two-handed grip, leaving one-handed weapons, bows, spells, and unarmed sheathing unchanged.
Integrated automatically with Versatile Weapons' live grip classification so native one-handed weapons gain the blend only in two-handed grip, while native two-handed weapons lose it when switched to one-handed grip.

Version 0.4.1
Blended the complete first-person presentation offset back to the vanilla position from 45% through 90% of melee, bow, and magic sheathing animations, letting adjusted arms and equipment retreat before the game hides the viewmodel.
Restored the configured offset smoothly when sheathing is interrupted, while leaving animation, gameplay sockets, aiming, and projectile origins unchanged.

Version 0.4.0
Reorganized FoA Mod Manager into General, Position, Equipment Depth, advanced held-melee and effect controls, and Diagnostics while keeping every stored config section and key unchanged.
Added friendly setting names, explicit ordering, visible units, signed direction guidance, and clearer explanations of equipment-specific and fallback offsets.

Version 0.3.9
Fixed equipped effects beneath the first-person body hierarchy being excluded from attached-effect alignment, which left vanilla torch flames and embers at their unadjusted position.
Kept cached effect emitters at the rendered-item offset between frames while compensating during the temporary body-root render translation, preventing local-space effects from receiving the offset twice.
Expanded diagnostics to report how many cached presentation effects use body-root render compensation.

Version 0.3.8
Added targeted first-person offsetting for Visual Effect Graph and particle-system presentation transforms beneath equipped hand items.
Kept gameplay roots, lights, colliders, rigidbodies, character controllers, sockets, and projectile origins unchanged while caching only the small active effect set.
Added AdjustAttachedEffects as a live master toggle for equipped presentation effects.

Version 0.3.7
Fixed held-heavy detection to follow the melee FSM's actual heavy-attack state without requiring its layer-activity flag, allowing two-handed charge corrections to engage when layer reporting lags the visible animation.
Expanded diagnostics to report every melee FSM's type, layer activity, general state, current state, target state, and held-mitigation result whenever that state signature changes.

Version 0.3.6
Changed the default held-melee offset scale to 1.0 and both held-only corrections to -0.05, matching the tuned pose.
Incremented the config schema because the held-melee scale, forward-correction, and vertical-correction defaults changed.

Version 0.3.5
Added a versioned, allocation-free visual-offset API for optional presentation integrations.
Cached the effective first-person world translation once per frame, including category offsets and active fireplace or held-melee blends, while reporting no offset when disabled, suspended, or in third person.

Version 0.3.3
Added held-melee extra forward and vertical offsets after the vanilla-return scale so charge poses that expose body geometry even at vanilla can be pulled into the camera near plane and lowered independently.
Defaulted the held-only correction to -0.10 forward and -0.06 vertical while retaining live adjustment from -0.50 to 0.50.
Refactored the held-melee transition into one shared zero-to-one mitigation blend so the retained-offset scale and both extra corrections engage and restore together.

Version 0.3.2
Added a configurable held-melee body-intrusion guard that smoothly returns the complete viewmodel offset toward vanilla while a heavy attack is raised or held.
Covered one-handed, two-handed, dual-wielded, and alternate melee heavy attacks through their shared active melee animation states while keeping Kandra arms, culling, ordinary geometry, and Drake weapons synchronized.
Restored the configured offset smoothly after release, cancellation, or another state transition instead of snapping the viewmodel.

Version 0.3.1
Added Linux/Proton installation support by packaging plugin folders and runtime assets with portable ZIP paths; Windows and Vortex installs remain unchanged.

Version 0.3.0
Converted the shared weapon translation into each linked Drake entity's own transform space so animated child rotations cannot turn one parent-space offset into inconsistent world motion.
Reduced multi-part and two-handed weapon jitter without adding temporal filtering, persistent smoothing state, or camera-response lag.

Version 0.2.9
Kept fireplace offsets at the vanilla position until the hero controller reports that its actual stand-up tween has completed, preventing the body from crossing the camera during the final transition frames.
Restored configured offsets with the existing smooth ease only after standing finishes, with a conservative timed fallback when the controller signal is unavailable.

Version 0.2.8
Smoothly blends configured viewmodel offsets back to the vanilla position during fireplace interactions so the crouched body does not move into view.
Keeps the vanilla position through cooking, rest, level-up, storage, fast travel, and other fireplace submenus without changing saved settings.
Delays and eases offset restoration to follow the fireplace stand-up animation instead of snapping when the menu closes.

Version 0.2.7
Restored the visually complete 0.2.0 rendering approach for Kandra arms and body meshes, Kandra culling data, and linked Drake weapons and held arrows.
Retained category offsets enabled by default with melee and magic at 0.30 and bows at 0.10.
Kept the later per-frame rig refresh and scene-transition cache cleanup safeguards that prevent stale first-person rig data from being reused after equipment or scene changes.
Removed active bow-state diagnostic hooks; bows now receive only their normal category offset with no special full-draw hand handling.

Version 0.2.6
Removed the temporary Kandra transform-capture path and its Harmony hooks after isolation testing identified it as the repeatable native crash trigger.
Added read-only hierarchy diagnostics that report camera masks, object layers, Kandra and Unity renderer paths, held weapon roots, hand sockets, and both pooled arrow variants when their state changes.
Preserved a customized Diagnostics value across this schema reset so an enabled diagnostic session remains enabled.
Incremented the config schema because the unsafe EnableKandraTransformCapture setting was removed.

Version 0.2.5
Removed the obsolete native Kandra buffer, culling-array, and Drake ECS mutation implementation from the source after the transform-capture replacement completed the transition test cleanly.
Cleared active bow diagnostic sampling on scene changes and rejected bow callbacks while capture is suspended, preventing stale transition references.
Disabled diagnostic logging by default for the stabilized transform-capture build.
Incremented the config schema because the Diagnostics default changed from true to false.

Version 0.2.4
Replaced direct Kandra native-buffer writes with a temporary first-person hierarchy shift around the game's own transform capture and upload.
Removed the unsafe Kandra culling and Drake ECS diagnostic switches and stopped installing the linked-transform ECS patch.
Restored the shifted hierarchy after Kandra uploads its buffer, with fallback restoration during update, disable, destruction, and scene changes.
Incremented the config schema because EnableKandraBoneOffsets was replaced by EnableKandraTransformCapture and the unsafe Kandra culling and Drake settings were removed.

Version 0.2.3
Replaced the scheduled Kandra bone-mutation job with a synchronous copied-matrix adjustment after the game's own bone-read job completes.
Ensured no custom Kandra bone job remains in flight across scene teardown while retaining first-person arm positioning.
Kept Kandra culling correction and Drake weapon offsets disabled after both native mutation paths independently reproduced the same crash.

Version 0.2.2
Decoupled Kandra bone adjustment from the Drake weapon switch so the native arm path can be tested independently.
Disabled Drake weapon offsets by default after isolation testing identified that path as the native crash trigger.
Enabled diagnostic logging by default for this isolation build so successful Kandra execution is visible in the log.
Incremented the config schema because the Drake weapon-offset and diagnostic logging defaults changed.

Version 0.2.1
Added independent diagnostic switches for Kandra bone offsets, Kandra culling correction, and linked Drake weapon offsets.
Disabled Kandra culling-array correction by default to isolate the leading native scene-transition crash candidate while retaining arm and weapon positioning.
Cleared cached Kandra, renderer, weapon, and ECS references on scene changes and briefly suspended native offsets while the replacement scene settles.
Added Kandra bone-region bounds checks, culling-array creation and length checks, and ECS world-lifetime validation before native access.

Version 0.2.0
Enabled category-specific forward offsets by default and changed the bow default to 0.10 while retaining 0.30 for melee and magic.
Moved UseCategoryForwardOffsets into the Core config section so the main category switch appears near the top.
Removed the experimental full-draw hand-depth correction and its two settings.
Incremented the config schema because the category switch moved sections, category and bow defaults changed, and the full-draw correction settings were removed.

Version 0.1.9
Added the matched pulling-hand Kandra bone count to bow diagnostics so adaptive correction coverage can be verified directly in the log.

Version 0.1.8
Added an adaptive full-draw depth correction that keeps the pulling hand in front of the camera while leaving the normal bow and body offset at 0.30.
Applied the correction only to the hand and finger bone subtree, with smaller weighted forearm and upper-arm adjustments to avoid exposing or moving the shoulders.
Derived the correction from the live hand-to-nock depth difference, blended it smoothly, capped it at 0.22, and left the bow, torso, projectile fire point, and gameplay transforms unchanged.

Version 0.1.7
Recorded both unmodified socket viewport positions and their render-adjusted positions in bow diagnostics so near-plane and screen-edge losses can be identified accurately.

Version 0.1.6
Added opt-in melee, bow, and magic forward offsets while retaining ForwardOffset as the default and unarmed fallback.
Added timed bow diagnostics for hand viewport depth, Kandra renderer visibility, arrow-pool activity, parenting, and nock-hand toggles through pull, hold, release, and idle transitions.
Kept all category defaults at 0.30 and left category overrides disabled by default so existing presentation remains unchanged.

Version 0.1.5
Removed cached late Kandra bone resampling and now resolves each active first-person rig region every frame, preventing disappearing arms or unrelated creature meshes after equipment changes.
Kept one late camera-offset sample for arms and rigid equipment without writing through stale Kandra buffer indices.
Included linked render entities beneath both animated hand sockets so both pooled bow-arrow variants receive the viewmodel offset.

Version 0.1.4
Late-resampled the first-person Kandra bones so hands, rigid weapons, and held arrows use the same post-LateUpdate animation pose and camera offset.
Corrected Kandra culling spheres and root matrices for the shifted first-person renderers so hands remain visible at full bow draw.
Kept the stable native Drake offset path and the 0.30 default without another config reset.

Version 0.1.3
Fixed linked swords, held arrows, and other Drake-rendered equipment flickering between adjusted and vanilla positions.
Applied the weapon offset before the game's linked-transform synchronization so rendering, bounds, and motion history consume one stable transform.
Preserved and restored each weapon prefab's original local-to-world offset when the mod is disabled or the item is unequipped.

Version 0.1.2
Moved linked Drake-rendered melee weapons, shields, and other equipped rigid models with the same visual offset as the first-person arms.
Changed the default ForwardOffset from 0.12 to 0.30.
Incremented the config schema because ForwardOffset now has a materially different default; customized compatible offsets remain recoverable.

Version 0.1.1
Moved Kandra-rendered first-person arms and equipment through their copied GPU bone matrices so the position settings affect the visible arm meshes.
Kept particle effects and non-Kandra geometry aligned through the existing render-time hierarchy offset.
Left the live animation skeleton, world FOV, aiming, projectile origins, and physics unchanged.

Version 0.1.0
Added an experimental render-time first-person body and weapon position offset without changing world FOV.
Added configurable forward, horizontal, and vertical offsets with a safe master toggle.
Restores the original first-person transform immediately after each main-camera render so gameplay transforms remain unchanged.

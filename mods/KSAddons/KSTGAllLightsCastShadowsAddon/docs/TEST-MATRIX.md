# KS All Lights Cast Shadows Addon Test Matrix

## Release smoke

| ID | Check | Expected result | Status |
| --- | --- | --- | --- |
| SHADOW-01 | Load a save with All Lights Cast Shadows and this addon enabled. | Version 2.0.5 loads without an exception and reports safe selection, atlas, and combat-aware protection active. | Not run |
| SHADOW-02 | Toggle the parent shadow mod off and on with Grail Floating Text installed, first with ShowToggleNotifications true and then false. | One `All Lights Cast Shadows: Disabled/Enabled` System notification confirms each actual change while true; no toggle notification appears while false. | Not run |
| SHADOW-03 | Visit an exterior with several point and spot lights, then inspect the log with Diagnostics and ShowGrailFloatingTextDiagnostics true; repeat with only the GFT setting false. | The scan always logs active, point, spot, estimated map, constrained, tracked, restored, and cap values; GFT shows one collapsed summary per changed count set only while both settings are enabled. | Not run |
| SHADOW-04 | Compare the same atlas-heavy view with ProtectShadowAtlas true and false. | The guard reduces promoted-light shadow resolution while enabled; disabling it immediately restores original HDRP resolution state. | Not run |
| SHADOW-05 | Move far enough that a constrained light leaves the parent budget, then return. | The light's original resolution override, tier, and override mode are restored when inactive and captured cleanly if promoted again. | Not run |
| SHADOW-06 | Use a light with an explicit resolution below PromotedShadowResolution. | The addon does not raise the light's authored lower resolution. | Not run |
| SHADOW-07 | Check a protected bonfire or campfire. | The fire illumination remains protected from self-shadowing as in version 1.2.0. | Not run |
| SHADOW-08 | Play with No Player Light installed and RespectExternalPlayerLightOwnership true, including with the parent's inactive-light option enabled. | The exact HeroLight hierarchy remains inactive and uncaptured, externalPlayerFaces remains zero, and other point/spot lights continue to be selected normally. | Not run |
| SHADOW-09 | Enter outdoor combat with the default combat settings. | The active atlas cap becomes 128 without changing the parent's budget or distance. | Not run |
| SHADOW-10 | End combat and wait five seconds. | A parent rescan restores the normal 256 atlas cap without rapid toggling during the delay. | Not run |
| SHADOW-11 | Fight inside an interior with OutdoorCombatOnly true, then false. | Combat overrides stay inactive while true and engage after the setting is disabled. | Not run |
| SHADOW-12 | Enable the combat budget and distance limits with parent values above and below their configured limits. | Each scan uses the lower value, never raises a stricter parent value, and restores the live parent config immediately afterward. | Not run |
| SHADOW-13 | Repeat combat entry and exit with Diagnostics enabled, first with ShowGrailFloatingTextDiagnostics true and then false. | Logs always report combat activation, restoration, and the exact enabled override set; collapsed GFT System notices appear only while both settings are enabled. | Not run |
| SHADOW-14 | With Diagnostics enabled, remain on the main menu and pass through a loading or teleport transition before entering gameplay. | Atlas summaries remain available in the BepInEx log, no atlas GFT summary appears outside active gameplay, and one collapsed summary appears after the gameplay scene is fully initialized. | Not run |
| SHADOW-15 | In a dense interior or town at night, enable Diagnostics and set the permanent limits to 16 lights, 25 metres, and 48 faces. | Selection never exceeds any of the three bounds; point lights count as six faces and spot lights as one, and unselected lights retain their authored state. | Not run |
| SHADOW-16 | Face a dense group of lights, slowly turn away and back, then move just beyond 25 metres but remain within the eight-metre retention margin. | View-relevant lights win initial selection, recently visible/selected lights remain stable through the configured grace and hysteresis, and no refresh performs more than two swaps. | Not run |
| SHADOW-17 | Activate Wyrd Sight around several highlighted items with ExcludeWyrdSightLights true, then false. | WyrdLight objects remain unmodified while true and become ordinarily eligible while false; the diagnostic exclusion count changes accordingly without an FPS collapse. | Not run |
| SHADOW-18 | Inspect a summon, character/item/object preview, lockpicking close-up, and player-placed portable Bonfire with their semantic exclusions enabled. | Each special light retains its authored shadow state and is counted in the matching diagnostic exclusion category. | Not run |
| SHADOW-19 | Test MageLight defaults in the reported problem interior; toggle it on/off, enter dialogue, switch perspective, and repeat with CastSoftShadows false. | HeroLight remains externally owned throughout, is never captured or restored by the addon, an active shadowed point light reserves six faces, and disabled shadows reserve zero without flicker or stale settings. | Not run |
| SHADOW-20 | Select a light that originally has HDRP shadows disabled, custom surface/volumetric dimmers, a custom resolution override, and a non-Every-Frame update mode; then make it leave selection. | Surface shadowing is enabled only while selected, added volumetric shadows are suppressed, every changed value is restored exactly, and the authored update mode never changes. | Not run |
| SHADOW-21 | With Diagnostics on, remain near the same light set for several seconds between parent scans. | Only the parent-triggered discovery performs a global Light search; 0.2-second view refreshes use the cache and do not cause repeated global discovery or full-state capture. | Not run |
| SHADOW-22 | Toggle UseSafeSelectionController and continue playing before restarting, then restart. | The current controller remains in force for the rest of the session with a restart notice; the requested safe or legacy compatibility path takes effect cleanly after restart. | Not run |
| SHADOW-23 | Traverse dawn and dusk first with ImproveDawnDuskShadows false, then true without Eyes in the Dark. | The disabled feature leaves the game's blend unchanged; enabling it applies the configured 10 in-game minutes per side without adding a directional light, and disabling it restores the captured value. | Not run |
| SHADOW-24 | Install MageLight and No Player Light together and reach the main menu with Grail Floating Text available. | The log and one deferred compatibility warning explain that MageLight can reactivate HeroLight; the addon leaves the hierarchy untouched and does not choose which external mod wins. | Not run |
| SHADOW-25 | Disable RespectExternalPlayerLightOwnership, rescan, then enable it again while MageLight or No Player Light is installed. | Disabling restores ordinary addon eligibility; re-enabling releases any captured HeroLight state and returns complete ownership to the external mod without activating the object. | Not run |
| SHADOW-26 | Enable ImproveDawnDuskShadows with Eyes in the Dark defaults, then observe daytime, quiet night, and maximum-threat night clock rates with Diagnostics enabled. | The applied blend follows the live rate at approximately 8, 37, and 19 in-game minutes per side respectively, targeting about 30 real seconds in every phase. | Not run |
| SHADOW-27 | With Eyes normalization active, traverse dawn and dusk while monitoring gameplay clock behavior and directional lights. | The shadow handoff is shorter and stable in real time, Eyes' own visual fades remain intact, gameplay time is unchanged, and no new directional light or shadow map appears. | Not run |
| SHADOW-28 | Enable the dawn/dusk feature, have another component change shadowIntensityBlendMinutes afterward, then disable the feature or unload the addon. | The addon logs one ownership warning, releases the field, and does not overwrite or restore the external component's newer value. | Not run |
| SHADOW-29 | Change scenes and exercise an additive scene transition while the dawn/dusk feature is active. | The old loaded DayNightSystem is compare-restored before transition, each newly loaded system is discovered once and captured independently, and normal quarter-second updates do not repeat the global component search. | Not run |

Version 0.1.4
Removed numeric prefixes from the generated config sections and added explicit FoA Mod Manager ordering for every visible setting.
Advanced ConfigSchemaVersion from 1 to 2 because settings moved to the clean General, Light Selection, Visuals, Notifications, and Diagnostics sections; this one-time cleanup regenerates defaults instead of guessing mappings from the retired section names.
Kept Diagnostics penultimate and Import Previous Settings last in the config menu.

Version 0.1.3
Increased the default interior contact-shadow budget from one stable light to four.
Added MaximumContactShadowLights so the active budget can be tuned from one to eight lights.
Preserved per-light hold timing, challenger advantage, and exact restoration across every selected light.

Version 0.1.2
Shows the enabled Grail Floating Text confirmation before the intercepted parent apply path consumes the runtime-state change.

Version 0.1.1
Added ShowToggleNotifications and standardized parent toggle confirmations, including the active interior-only scope.
Keeps ordinary interior and exterior transitions silent.

Version 0.1.0
Added interior-only contact shadows by default with exact light, camera, and volume restoration when inactive.
Replaced broad per-light activation with one stable nearby point or spot light at a time.
Added minimum hold time and challenger advantage rules to reduce dominant-light switching and visible flicker.
Excluded inactive, disabled, directional, area, zero-intensity, and zero-range lights from selection.
Added conservative visual defaults with configurable distance, sample count, ray length, and opacity without editing the parent JSON.
Added Grail Floating Text notifications for the parent mod's runtime toggle.

TargetVersion=2.2.3
BaselineVersion=2.2.2
Routed every replacement layer through the game's Music mixer so the Master and Music volume controls and mute states apply. Replacement playback now fails closed if the Music bus is unavailable instead of bypassing the game mixer.
